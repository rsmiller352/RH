---
id: THM_016_003
publication_dependencies: ["DEF_003_003","THM_016_001"]
---

# Canonical Intrinsic Three-Rank Quadratic-Support Spectrum

## 0. Governance and canonical theorem claim

This governed formal card is the governed successor of the frozen alias
`THM_LM_INTRINSIC_LEPTON_SPECTRUM_001`. Its one theorem claim is:

> The intrinsic \(D_3\)-orbit has a positive, strictly ordered
> minimum/middle/maximum amplitude triple. The construction-derived counting
> adjoint and complete retained CAP tensor book uniquely force the same-root
> quadratic-support contraction
> \(M_{\rm read}(A)=mA^\dagger A\) with the sole coefficient
> \(m=1/(2\pi)\); hence the intrinsic support triple and its two exact ratios
> are presentation-invariant.

No particle identity, laboratory datum, SI unit, finite-screen factor, or
measured value occurs in this theorem.

## 1. Intrinsic amplitude data

From `THM_016_001`,

\[
\operatorname{Spec}_{\rm unord}\mathfrak A_{\rm int}(o)
=
\left\{
1+\sqrt2\cos\!\left(
\frac29+\frac{2\pi k}{3}
\right):
k\in\mathbb Z/3\mathbb Z
\right\}.
\]

For the reference calculation set

\[
\alpha=\frac29
\]

and

\[
\begin{aligned}
\Lambda_0&=1+\sqrt2\cos\alpha,\\
\Lambda_1&=
1-\frac{\cos\alpha}{\sqrt2}
-\frac{\sqrt6}{2}\sin\alpha,\\
\Lambda_2&=
1-\frac{\cos\alpha}{\sqrt2}
+\frac{\sqrt6}{2}\sin\alpha.
\end{aligned}
\]

These reference indices are not intrinsic sector names. The intrinsic values
are defined by rank:

\[
\Lambda_{\min}:=\min\operatorname{Spec}_{\rm unord}\mathfrak A_{\rm int}(o),
\]

\[
\Lambda_{\rm mid}:=
\text{the unique element strictly between the minimum and maximum},
\]

\[
\Lambda_{\max}:=\max\operatorname{Spec}_{\rm unord}\mathfrak A_{\rm int}(o).
\]

## 2. Exact positivity and strict rank

The elementary bounds

\[
0<\frac29<\frac{\pi}{12}<\frac{\pi}{3}
\]

give \(\sin\alpha>0\) and
\(\sqrt3\cos\alpha-\sin\alpha>0\). Therefore

\[
\Lambda_2-\Lambda_1
=
\sqrt6\sin\alpha>0
\]

and

\[
\Lambda_0-\Lambda_2
=
\sqrt{\frac32}
\left(
\sqrt3\cos\alpha-\sin\alpha
\right)>0.
\]

Moreover,

\[
\frac{2\pi}{3}+\alpha<\frac{3\pi}{4},
\]

so monotonicity of cosine on \([0,\pi]\) yields

\[
\Lambda_1
>
1+\sqrt2\cos\frac{3\pi}{4}
=0.
\]

Thus

\[
\boxed{
0<\Lambda_{\min}<\Lambda_{\rm mid}<\Lambda_{\max}.
}
\]

In the reference display only,

\[
(\Lambda_{\min},\Lambda_{\rm mid},\Lambda_{\max})
=
(\Lambda_1,\Lambda_2,\Lambda_0).
\]

Every permitted presentation merely permutes the displayed sector indices,
so the rank triple is presentation-invariant.

## 3. Forced CAP-native adjoint-contraction proof

This section reproduces the complete load-bearing proof from SCM-002 and
LM-CG-006-R2. SCM-002 constructs the free complex linearization, its canonical
counting Hermitian form, and its adjoint. LM-CG-006-R2 applies that already
fixed duality to the complete retained CAP tensor book. No relational
co-reading operation is retyped as an adjoint.

Let

\[
C=\{0,1,2\},
\qquad
V=\mathbb C[C],
\qquad
J=C\times C,
\]

and let \(E_{rp}=|r\rangle\langle p|\) be the canonical matrix units for the
counting Hermitian form. Define

\[
U_J=\sum_{r,p\in C}E_{rp},
\qquad
A=\sum_{r,p\in C}A_{rp}E_{rp}.
\]

Matrix-unit multiplication gives

\[
E_{rp}^{\dagger}E_{sq}
=
E_{pr}E_{sq}
=
\delta_{rs}E_{pq},
\]

and hence

\[
\operatorname{Tr}
\left(E_{rp}^{\dagger}E_{sq}\right)
=
\delta_{rs}\delta_{pq}.
\]

The three cyclic-difference classes sum to
\(\mathbf1_V,P,P^{-1}\), so

\[
U_J=\mathbf1_V+P+P^{-1}=3\Pi_0.
\]

Therefore

\[
U_J^\dagger U_J=9\Pi_0,
\qquad
\operatorname{Tr}(U_J^\dagger U_J)=9=\operatorname{Cap}.
\]

The free linearization supplies the basis identifications

\[
\mathbb C[J]
\cong
V_{\rm root}\otimes V_{\rm position}^{*}
\cong
\operatorname{End}(V),
\qquad
(r,p)\longmapsto e_r\otimes e^p\longmapsto E_{rp}.
\]

The counting form fixes

\[
E_{rp}^{\dagger}=E_{pr}
\]

and the root-leg evaluation

\[
\overline{e_r}\otimes e_s\longmapsto\delta_{rs}.
\]

For a weighted record, the construction-derived adjoint co-record and the
canonical partial evaluation give

\[
\begin{aligned}
A^\dagger A
&=
\left(
\sum_{p,r}\overline{A_{rp}}E_{pr}
\right)
\left(
\sum_{s,q}A_{sq}E_{sq}
\right)\\
&=
\sum_{p,q}
\left(
\sum_r\overline{A_{rp}}A_{rq}
\right)
E_{pq}.
\end{aligned}
\]

This contraction evaluates only the repeated root leg and retains both
position legs \(p,q\). Hence

\[
\boxed{
M_{\rm read}(A)=mA^\dagger A.
}
\]

This map is unique from the constructed data. A cross-root kernel would insert
a new tensor, tracing and redistributing would erase the retained position
legs, and nonlinear post-processing would no longer be the CAP tensor
contraction.

The sole operative coefficient is

\[
\boxed{
m=\frac1{2\pi}>0.
}
\]

For \(x,y\in V\),

\[
\langle x,M_{\rm read}(A)y\rangle
=
m\langle Ax,Ay\rangle,
\]

so nondegeneracy makes the representative unique. It also gives

\[
M_{\rm read}(BA)=A^\dagger M_{\rm read}(B)A,
\qquad
M_{\rm read}(\mathbf1_V)=m\mathbf1_V.
\]

For comparison, a further positive-real power law

\[
\mathcal F_r(B)=mB^r,
\qquad r\in\mathbb R_{>0},
\]

on the positive-semidefinite CAP-pairing record, then the spectral power is
well-defined even though the record has a kernel, and

\[
\operatorname{Spec}(U_J^\dagger U_J)=\{9,0,0\}
\]

and full-book closure requires

\[
m9^r=9m.
\]

Since \(m>0\), \(9>1\), and \(r\mapsto9^r\) is strictly increasing on
\(\mathbb R_{>0}\), this forces \(r=1\). Thus, within the stated positive-real
power-law family, the only response is the already-forced adjoint pairing; no
extra exponent is fitted. No negative-power extension through the kernel and
no \(0^0\) convention is asserted.

## 4. Intrinsic quadratic supports

Each \(A^{\rm scr}_{\sigma,j}\) is Hermitian, so

\[
M_{\rm read}
\left(A^{\rm scr}_{\sigma,j}\right)
=
m\left(A^{\rm scr}_{\sigma,j}\right)^2.
\]

The transported conjugacy from `THM_016_001` gives

\[
M_{\rm read}\!\left(A^{\rm scr}_{\sigma,j}\right)
=
U_{\sigma,j}
M_{\rm read}\!\left(A^{\rm scr}_{+,0}\right)
U_{\sigma,j}^{\dagger}.
\]

Therefore define the intrinsic quadratic-support orbit by

\[
\boxed{
\mathfrak M_{\rm int}(o)
:=
\left[
M_{\rm read}\!\left(A^{\rm scr}_{\sigma,j}\right)
\right]_{D_3}.
}
\]

Its eigenvalues are \(m(\lambda^{\rm scr}_{\sigma,j;k})^2\). The intrinsic
unordered support spectrum is therefore

\[
\boxed{
\operatorname{Spec}_{\rm unord}
\mathfrak M_{\rm int}(o)
=
\left\{
m\Lambda_{\min}^2,\;
m\Lambda_{\rm mid}^2,\;
m\Lambda_{\max}^2
\right\}.
}
\]

Define

\[
\boxed{
q_{\min}:=m\Lambda_{\min}^2,
\qquad
q_{\rm mid}:=m\Lambda_{\rm mid}^2,
\qquad
q_{\max}:=m\Lambda_{\max}^2.
}
\]

Because \(m>0\) and the amplitudes are positive and strictly ordered,

\[
\boxed{
0<q_{\min}<q_{\rm mid}<q_{\max}.
}
\]

No observer averaging is used. Unitary covariance gives the same unordered
support spectrum in every permitted presentation.

## 5. Exact dimensionless ratios

The coefficient cancels:

\[
\boxed{
R_{\rm mid/\min}^{\rm int}
:=
\frac{q_{\rm mid}}{q_{\min}}
=
\left(
\frac{\Lambda_{\rm mid}}{\Lambda_{\min}}
\right)^2,
}
\]

\[
\boxed{
R_{\max/\min}^{\rm int}
:=
\frac{q_{\max}}{q_{\min}}
=
\left(
\frac{\Lambda_{\max}}{\Lambda_{\min}}
\right)^2.
}
\]

These exact symbolic expressions are the theorem results. They are independent
of the common coefficient \(m\), presentation frame, and reporting unit.

For drift diagnosis only, not proof or definition, the frozen evaluations are

\[
R_{\rm mid/\min}^{\rm int}
=
206.77031597272938861255033931777507746217879491003\ldots,
\]

\[
R_{\max/\min}^{\rm int}
=
3477.4728371045985323130012255226483445993318601992\ldots.
\]

## 6. Single-\(m\) authority discipline

This theorem consumes only the active local defining range
`DEF_003_003:L24-L38`, where

\[
m=\frac1{2\pi}.
\]

`SYM_016_SINGLE_M` is the SEC016 context record for this same sole
coefficient authority. It is not a second coefficient type. The
Batch-1-only symbol record is not imported because its allowed-use scope does
not license an SEC016 promotion.

It neither consumes nor identifies any other coefficient role, and it does
not use the physical-energy Bridge. `SRC_000227:L34-L55` establishes that the
repository-wide coefficient-ontology migration and the legacy energy-gate
repair are separate controlled work, not blockers for this dimensionless
theorem.

## 7. Forced-step chain

1. `THM_016_001` supplies the intrinsic unordered amplitude spectrum.
2. Exact trigonometric differences force positive strict rank.
3. Matrix-unit orthogonality fixes the complete nine-incidence CAP book.
4. SCM-002's counting duality and the complete CAP tensor address force the
   unique same-root contraction retaining both position legs,
   \(A^\dagger A\).
5. Full-book closure rejects every additional positive-real response power
   \(r\ne1\) within \(\mathcal F_r(B)=mB^r\).
6. Positivity of the single coefficient preserves rank under
   \(m\Lambda^2\).
7. Ratio formation cancels the common coefficient exactly.
8. \(D_3\) covariance preserves the unordered support triple and rank.

## 8. Exact source-to-theorem map

| Formal content | Exact Source Card range |
|---|---|
| rooted support, radius, cubic and symmetric algebra | `SRC_000216:L37-L202,L269-L395` |
| ordered amplitudes, positivity, and rank | `SRC_000217:L284-L385` |
| presentation covariance | `SRC_000218:L32-L140,L264-L280` |
| compact formal CAP proof | `SRC_000220:L12-L116` |
| CAP scope and falsifiers | `SRC_000220:L118-L136` |
| canonical free linearization, counting form, adjoint, and Hermitian family | `SRC_000229:L20-L100,L149-L157` |
| complete CAP book, contraction, pullback, exponent, spectrum, and covariance | `SRC_000230:L9-L306` |
| tensor contraction and uniqueness against added kernels or erasure | `SRC_000231:L6-L177` |
| data-free rank/ratio lock and nonclaims | `SRC_000221:L12-L45` |
| selected frozen consolidation | `SRC_000222:L23-L145` |
| Route-B and single-\(m\) authorial authority | `SRC_000219:L465-L518` |
| active local definition \(m=1/(2\pi)\) only | active `DEF_003_003:L24-L38` |
| local nonblocking scope and separate legacy migration | `SRC_000227:L34-L55` |
| correction preserving the derived CAP-native law | `SRC_000228:L42-L44,L59-L60,L104-L117` |

## 9. Scope, fences, and falsifiers

This theorem makes no name attachment and no dimensional report. It fails if:

1. a labeled screen sector is treated as intrinsic;
2. a non-quadratic support power is used;
3. the common coefficient is free, duplicated, or fitted;
4. a decimal replaces an exact symbolic claim;
5. any laboratory or measured record enters;
6. any finite-screen deformation is applied here;
7. presentation covariance changes the unordered rank triple; or
8. any cited source or predecessor hash changes.

It also fails if the full tensor address and construction-derived adjoint are
replaced by CAP cardinality alone, or if the positive-real exponent domain is
silently enlarged to include undefined negative powers through the kernel, an
unlicensed \(0^0\) convention, or complex powers.

The exact witness runs real corruptions for these failures.

External limitation: the repository-wide coefficient-ontology migration and
legacy physical-energy gate repair remain required in their own governance
wave. They do not enter this theorem's proof or dependency closure.

`PROMOTED-BY-GOVERNED-TRANSACTION`.
