---
id: THM_004_021
publication_dependencies: ["DEF_002_002","DEF_002_004","LEM_003_010","LEM_004_006","THM_003_012"]
---

# The S02→S03 seam: the FCC edge IS the minimal 2-channel coupling — DERIVED relationally, discharging THM_S03_024's (P2) FIAT. Earned content: (a) (P2)-from-transfer (a cross-cell connection is a transfer, A≠B = 2 registers, DEF_002_004; channels=registers, THM_003_012 ⟹ an edge couples exactly 2 of the c=3 channels, 1 idle); (b) the EQUIVARIANT IDENTITY (output, NOT cited): the channel-coupling action G₃ (LEM_003_010) and the FCC-shell action O_h (DEF_004_001) are the SAME transitive action — identical stabilizers, equivariant iso-count = |N_G(H)/H| = 2 = |Aut| (an orientation automorphism) — so in the content-free relational framework iso = IDENTITY, hence G₃ ≅ O_h is DERIVED HERE and CONV_S03_003's channels↔axes map demotes to a PRESENTATION. η = √2 is a relational RATIO (Schur: the O_h-invariant metric on ℝ³ is dim-1, forced up to a content-free scale; the √2 in D = π/(3√2)). PRE-METRIC, all relational

## 0) Governance & Scope Lock

**THM card (S02→S03 seam) — the FCC edge IS the minimal 2-channel coupling, DERIVED relationally; discharges `THM_S03_024`'s (P2) FIAT.** PRE-METRIC / purely relational (TD-019: no distance at this layer). The cross-cell transfer-edge (`A≠B`, `DEF_002_004`) couples exactly 2 of the c=3 channels (1 idle; `THM_003_012` channels=registers + 3-cycle; `THM_002_011` named). The **channel-coupling action `G₃`** (`LEM_003_010`, on the labels) and the **FCC-shell action `O_h`** (`CONV_S03_003`, on `S_12`) are the **SAME transitive action** — identical stabilizers; the equivariant iso-count = `|N_G(H)/H| = 2 = |Aut|` (an orientation automorphism, `|G|=48`, `|H|=4`, `|N_G(H)|=8`) — so in the content-free relational framework **iso = IDENTITY** (verifier F-281b). Therefore **`G₃ ≅ O_h` is DERIVED HERE** (output, not cited), the channel↔axis correspondence is **forced by equivariance** (so `CONV_S03_003`'s map demotes to a **presentation**), **η = √2 is a relational RATIO** (Schur: the `O_h`-invariant metric on ℝ³ is dim-1, forced up to a content-free scale; the √2 in `D = π/(3√2)`), and **FCC is the UNIQUE relational 12-contact structure**. No new glyph; η's value unchanged.

Banned: consuming `DEF_S03_004`/`CONV_S03_003`'s channel↔axis map as input (the 12 is built from the channels; `DEF_S03_004` grounded, the map demoted; `CONV_S03_003`'s ambient/`O_h` license is fine upstream context); `12 = 3·4` as input (the 12 is the orbit size = output); metric/length/coordinate content (pre-metric; η a ratio); η-value change / power law; signed/σ-arithmetic/zero; minting "B₃" (use `G₃`/`O_h`); editing any promoted card IN this card (the (P2) + `CONV_S03_003` amendments are the bundle's separate `--amend` half); reliance on `THM_003_012`'s staged amendment.

## 1) Plain language

What *is* a connection between cells? A **transfer** — and a transfer needs a source and a target, **two distinct registers** (`DEF_002_004`: A≠B). The connectivity is the 3-cycle of registers; the registers ARE the c=3 channels (`THM_003_012`). So **each edge couples exactly two of the three channels, leaving one idle** — a single channel is not a transfer (no target), so it is not an edge. That is exactly the "no axis-parallel ports" that `THM_S03_024` had to *assume* (its (P2) fiat); here it is **derived.**

Now the two pictures of these couplings — the **channel picture** (`G₃`, relabelings of the channel *labels*) and the **coordinate picture** (`O_h`, signed permutations of the `(±1,±1,0)` *coordinates*) — are not two structures that "look alike." They are the **same transitive action**: same number of objects (12), same symmetry pattern, the *same stabilizer* of each object. The honest check: between two structures of this kind, the number of **structure-preserving** maps equals the structure's own symmetry count — here **two** (a left/right orientation flip), not the 48 group elements (counting elements was the earlier error). There is no third structure to choose, no "which channel is which axis" decision with any content behind it.

Because the framework has **no distance, no metric** — only relational structure — "the same structure" *is* "the same object." So `G₃ ≅ O_h` is **derived here** (not borrowed from a convention), the coordinate map (`CONV_S03_003`) drops to a mere labelling, the `(±1,±1,0)` picture is just a *drawing*, and the `√2` is a **ratio** (the dimensionless number in the packing fraction `D = π/(3√2)`), not a length. The FCC edge **is** the 2-channel coupling — forced by the nature of the space-filling structure itself.

## 2) Statement

> **(THM_004_021 — the FCC edge IS the minimal 2-channel coupling; (P2) discharged.)** In the purely relational, pre-metric setting (c=3 channels; the persisting 3-cycle; no distance, TD-019):
>
> - **(1 — (P2) DERIVED.)** A cross-cell connection is a transfer `T(A→B)` with **A ≠ B** (`DEF_002_004 §2`) — exactly **2 distinct registers**. Registers = channels (`THM_003_012`). So an **edge couples exactly 2 of the c=3 channels** (1 idle); a **1-channel (axis-parallel) object is NOT an edge** (no A≠B); the 3-channel object is the closure (cycle), not an edge. This **discharges `THM_S03_024`'s (P2) FIAT** to a theorem.
> - **(2 — the channel-coupling action.)** With orientations (σ, `DEF_002_002`) at minimal units, the 2-channel couplings form a **transitive action of `G₃`** (the channel-relabeling group `S₃⋉2³` of order 48, `LEM_003_010`), 12 objects, point-stabilizer of order 4 — the `(a,a,0)`-type (at unit presentation, a=1).
> - **(3 — the FCC-shell action.)** The unique size-12 **orbit-TYPE** of `O_h` is `(a,a,0)` (`LEM_004_006`); at a=1 this orbit IS `S_12`, its representative (1,1,0) of norm²=2 (η = √2, the coupling's own norm) — a **transitive action of `O_h`** (the order-48 coordinate symmetry, `DEF_004_001`), 12 objects, point-stabilizer of order 4.
> - **(4 — the EQUIVARIANT IDENTITY, derived; and its consequences.)** The actions (2) and (3) have **identical stabilizers** ⟹ they are the **same transitive action**; the equivariant iso-count = `|N_G(H)/H| = 2 = |Aut|` (an orientation automorphism, `|G|=48`, `|H|=4`, `|N_G(H)|=8`), NOT `|G|=48`. In the **content-free relational framework, iso = IDENTITY**. Hence **`G₃ ≅ O_h` is DERIVED HERE** (output), **the FCC edge IS the minimal 2-channel coupling**, the channel↔axis correspondence is **forced by equivariance** ⟹ **`CONV_S03_003`'s map demotes to a PRESENTATION**, **η = √2 is a relational RATIO** (Schur: the `O_h`-invariant metric on ℝ³ is dim-1, forced up to a content-free scale; the same √2 in `D = π/(3√2)`), and the 12 couplings generate Λ_FCC (the generation `span_ℤ S_12 = Λ_FCC` is completed downstream at `THM_004_027`; not consumed here).
>
> Derived by **(P2)-from-transfer + orbit-type uniqueness + the equivariant identity** — NOT the declared `CONV_S03_003` map, NOT the `12 = 3·4` count (the 12 is the orbit size, an output). η's value unchanged (`THM_004_019`). √2, π symbolic (P8).

## 3) Input contract (GR-1)

**Consumed (5 edges):** the transfer A≠B = 2 registers ← `DEF_002_004`; channels=registers, c=3, the 3-cycle content ← `THM_003_012`; the channel-relabeling group `G₃` = `S₃⋉2³` ← `LEM_003_010`; σ ← `DEF_002_002`; the unique `(a,a,0)` orbit-type + `O_h` orbits (S_12 IS this orbit at a=1) ← `LEM_004_006`. **Named (NOT input):** `THM_004_009` (FCC generation `span_ℤ S_12 = Λ_FCC` — forwarded to `THM_004_027`, not consumed here); `THM_002_011` (3-cycle — delivered via `THM_003_012`); `DEF_S03_004` (12-port hypothesis — grounded, not consumed); `CONV_S03_003` (channel↔axis map demoted to presentation; its ambient/`O_h` license = upstream context, not a premise here); `THM_S03_024` ((P2) discharged — amended separately); `THM_004_019` (η value, unchanged); `GUARD_003_009`/`GUARD_004_017` (D-037 fence, cleared); `LEM_002_006` (location relational). **Domains:** relational, pre-metric; finite group/orbit (exact integers) + Schur (exact QQ). **No metric/length; no 12=3·4 input; no declared-map input; no signed/zero; no η-value change; no un-registered glyph.**

## 4) Proof (complete — relational, pre-metric)

**Step 1 — (P2) from the transfer relation.** By `DEF_002_004 §2`, a transfer `T(A→B)` requires **registers A ≠ B** (a 1-register form is ill-typed — no target). The connectivity is the persisting 3-cycle (named via `THM_003_012`, which carries `THM_002_011`'s closure content), whose edges are the transfers A→B, B→C, C→A. By `THM_003_012`, c = the minimal persisting-closure register count, so **channels = registers**. Hence an **edge couples exactly 2 of the 3 channels** (1 idle); a 1-channel object is not a transfer (no A≠B), so not an edge; the 3-channel object is the cycle, not an edge. **(P2) — "no axis-parallel ports" — DERIVED**, discharging `THM_S03_024`'s fiat. ∎(1)

**Step 2 — the channel-coupling action.** With orientations σ (`DEF_002_002`, each channel ±) at minimal units, the 2-channel couplings = {choose the idle channel (3) ; sign each active (2²)}, 12 objects = the `(a,a,0)`-type at a=1. The symmetry is the channel-relabeling group `G₃` (channel permutations S₃ × per-channel orientation flips 2³, order 48; `LEM_003_010`). The action is transitive (any coupling maps to any other by relabeling+flip); the point-stabilizer `H` of one coupling has order `48/12 = 4`. ∎(2)

**Step 3 — the FCC-shell action.** By `LEM_004_006`, the unique size-12 **orbit-type** of `O_h` is `(a,a,0)`; at a=1 this orbit IS `S_12`, its representative (1,1,0) of norm²=2 (η = √2, the coupling's own norm ratio — NOT a shell-selection). `S_12` is a transitive `O_h`-action (order 48), 12 objects, point-stabilizer `H'` of order 4. ∎(3)

**Step 4 — the equivariant identity (output) ⟹ the forced consequences.** The two transitive actions (2), (3) are of the same abstract group (`S₃⋉2³`, order 48) with the **same point-stabilizer order (4)** and the same stabilizer structure (the subgroup fixing one "2-active, 1-idle/2-nonzero, 1-zero" object) ⟹ **the same transitive action** (a transitive G-action is determined by its point-stabilizer up to conjugacy). The number of **equivariant** bijections between them is `|N_G(H)/H| = |Aut| = 8/4 = 2` (an orientation automorphism) — **NOT** `|G| = 48` (that counts group elements / arbitrary set-maps; the F-281 error, corrected by F-281b). The framework is **pre-metric / purely relational** (TD-019: no distance; no content beyond structure). Two transitive actions that are the same up to a 2-element automorphism, with **no extra distinguishing content**, ARE the same object (**iso = identity**). Therefore: **(a)** `G₃ ≅ O_h` is **DERIVED HERE** (not cited — `LEM_003_010` names `G₃` on labels only and bans the spatial reading; `O_h` is `CONV_S03_003`'s coordinate glyph); **(b)** the FCC edge **IS** the minimal 2-channel coupling, the channel↔axis correspondence **forced by equivariance** ⟹ `CONV_S03_003`'s map demotes to a **presentation** (content-free up to the 2-fold orientation); **(c)** η = √2 is a relational **ratio** — by Schur, the space of `O_h`-invariant symmetric bilinear forms on ℝ³ is dimension 1 (the standard rep is irreducible), so the metric is forced up to a content-free scale, and √2 = √(min-norm²/axis-norm²) is scale-invariant (the same √2 in `D = π/(3√2)`); **(d)** FCC is the unique relational 12-contact structure; the generation `span_ℤ S_12 = Λ_FCC` is completed downstream at `THM_004_027` (forwarded, not consumed here). ∎(4)

**What this proof does NOT do / claim:** no metric/length/absolute-distance (pre-metric; η a ratio); no consumption of `DEF_S03_004`'s 12-port hypothesis or `CONV_S03_003`'s channel↔axis map as input (built from channels; matched by orbit-type + equivariance); no `12=3·4` as a premise (the 12 is the orbit size); no η-value change (`THM_004_019`); no signed/σ-arithmetic/zero; no un-registered glyph; no edit of any promoted card in this card (the (P2) + `CONV_S03_003` amendments are the bundle's separate `--amend` half).

## 5) Scope & non-overclaim

Establishes: the FCC edge = the minimal 2-channel coupling (relationally, pre-metric); (P2) discharged; the equivariant identity `G₃ ≅ O_h` (derived output); `CONV_S03_003`'s channel↔axis map → presentation; η = √2 a relational ratio; FCC the unique relational 12-contact structure. Does NOT: introduce metric/length (pre-metric); consume `DEF_S03_004` or `CONV_S03_003`'s map as input, or the `12=3·4` count; change η's value or write a power law; mint an un-registered glyph; edit any promoted card in this card (the amendments are separate `--amend`); rely on `THM_003_012`'s staged amendment; assert any spacetime/physical reading (gated/forward, P0/P11); weaken `CONV_S03_003`'s ambient/`O_h` license (six cards consume it — untouched). Guard references: `GUARD_003_009`/`GUARD_004_017` (D-037, cleared), `GUARD_001_005` (no σ-arithmetic), `LEM_002_006` (location relational), `THM_004_019` (η unchanged). The downstream chain (`b=η·a`, `Ψ=η·S_flow`, the Koide √2) is named reference (P11), grounded BY this theorem but built elsewhere.

## 6) Per-symbol accounting

**Introduced: none** (`symbols_introduced: []`; relates promoted objects; `O_h` registered; `G₃` is `LEM_003_010`'s card-local name for the channel-relabeling group; the abstract group is named "the order-48 relabeling group `S₃⋉2³`" in prose — NO "B₃" minted, P9). **Consumed as ground (5 edges):** see §3. **Registry symbols used:** η (`THM_004_019`), c (`DEF_003_005`), σ (`DEF_002_002`), `O_h` (`DEF_004_001`). **Named (NOT consumed):** `THM_002_011`, `DEF_S03_004`, `CONV_S03_003` (map demoted; ambient license upstream), `THM_S03_024`, `THM_004_019`, `GUARD_003_009`/`GUARD_004_017`, `LEM_002_006`. **Undeclared: none. No metric symbol; no new glyph; √2, π symbolic (P8); no decimal except a [COMPARISON OUTPUT]-marked pairing.**

## 7) Verification (witness SPEC — independent author; P12: mode EXACT (finite group/orbit, integers; Schur in exact QQ); reads live ground, TD-030; NO hardcoded booleans, F-256; TD-035 disjoint; run lint_witnesses.py)

Modes A–E each flip exactly one check (re-authored independently of the F-281b scripts):
1. **(P2)-from-transfer (Mode A, LB — F-S03seam-1):** `DEF_002_004 §2` requires A≠B (a 1-register transfer ill-typed); channels=registers (`THM_003_012`, promoted text) ⟹ edge couples 2. Corrupt to admit A=B ⟹ fails.
2. **equivariant identity (Mode B, LB — F-S03seam-2, the corrected core):** COMPUTE the equivariant iso-count between the channel-coupling action (`G₃`) and the `S_12` action (`O_h`) = `|N_G(H)/H| = 2` (`|G|=48`, `|H|=4`, `|N_G(H)|=8`), NOT 48; identical point-stabilizers (same transitive action). Corrupt to count set-maps/group-elements ⟹ the 48 reappears ⟹ fails.
3. **η a relational ratio (Mode C — F-S03seam-3):** η = √2 scale-invariant (rescale λ ∈ {1,3,1/7,100}); the `O_h`-invariant symmetric bilinear form on ℝ³ is dim-1 **in exact Sage QQ** + the explicit ∝ I form. Corrupt to a 2-parameter form ⟹ fails.
4. **orbit-type + (P2)-exclusion (Mode D — F-S03seam-4):** the `(a,a,0)` `O_h`-orbit = `S_12`; channel-2-coupling set (3 idle × 4 signs) = `S_12`; axis-parallel (±1,0,0) ∉ Λ_FCC. Corrupt ⟹ fails.
5. **no circular consumption (Mode E — F-S03seam-5):** no `12=3·4` premise; `DEF_S03_004` / `CONV_S03_003`'s channel↔axis map not consumed (dependency-input audit). Corrupt to inject ⟹ fails.

**Can-fail (--corrupt):** bare → the crux Mode A; each mode flips ONE check (TD-035); the bijection A→1…E→5; normal = 0. Modes A/B/D read live ground (TD-030); Mode C is the exact-QQ Schur. **Falsifier-signing (TD-041):** each LB falsifier fires when the claim is FALSE. Run `lint_witnesses.py`; `last_pass_hash: null`; `promote_card.py --dry-run`.

## 8) Falsifiers

As frontmatter — **F-S03seam-1** ((P2)-from-transfer — Mode A, LB), **F-S03seam-2** (the equivariant identity / the 2=|Aut| correction — Mode B, LB), **F-S03seam-3** (η a length not a ratio — Mode C), **F-S03seam-4** (orbit-type — Mode D), **F-S03seam-5** (circular consumption / fence — Mode E). A THM with constitutive falsifiers (D-025.R25): the (P2) discharge can lose (if a transfer doesn't need A≠B) and the equivariant identity can lose (if the equivariant iso-count exceeds |Aut|, i.e. distinguishing content exists). **Falsifier-signing (TD-041):** each LB falsifier fires when the claim is FALSE (F-281b's correction is itself the witness Mode B). Loss handling per D-025.R26/D-042: the consumed promoted facts hold; a violation diagnoses THIS card. **Reopen:** an RSM re-ruling on the channels/geometry seam, or `THM_S03_024` reopening.

## 9) Notes

**(i) The crux confirmed FORCED (F-281b; F-281 retracted).** The verifier first refuted ("free choice of 48 isos = CONV_S03_003"), then **self-corrected** (F-281b, class math-error): the **equivariant** iso-count is `|N_G(H)/H| = 2 = |Aut|`, not 48 = `|G|` (which counts group elements, not structure-maps). The two actions are the SAME transitive action (identical stabilizers); the residual 2 is an orientation automorphism. RSM's relational reframe ("no metric; all relational; forced by the nature of FCC space-filling spheres") is what exposed the error.

**(ii) iso = identity (the relational license, TD-019).** In a pre-metric framework with no content beyond structure, two isomorphic transitive actions with no distinguishing content ARE the same object. This is LICIT here (and is exactly why `CONV_S03_003`'s map demotes to a presentation); the prior "iso ≠ identity" (TD-019/TD-021 in its usual direction) is the over-application F-281b corrected — that guard fires when there IS extra (e.g. metric) content, which here there is not.

**(iii) `G₃ ≅ O_h` is DERIVED, not cited (the architect's Finding 1).** `LEM_003_010` names `G₃` (the channel-relabeling group on *labels*, order 48) and bans the spatial reading; `O_h` (`DEF_004_001`) is the *coordinate* symmetry. THIS theorem derives they are the same transitive action (the equivariant identity), hence the abstract iso `G₃ ≅ O_h` — as OUTPUT, never the `CONV_S03_003` convention re-imported. No "B₃" is minted (the abstract group is named in prose).

**(iv) η = √2 is a ratio (Schur).** The `O_h`-invariant symmetric bilinear form on ℝ³ is dim-1 (standard rep irreducible), so the metric is forced up to a content-free scale; η = √(min-norm²/axis-norm²) = √2 is scale-invariant, the same √2 in the dimensionless packing fraction `D = π/(3√2)`. No metric unit; pre-metric honored.

**(v) The amendment bundle (separate --amend, sequenced; lightest CONV_S03_003 touch).** This THM grounds, but does not edit: `THM_S03_024`'s (P2) FIAT → discharged-by-THM_004_021 (the §8/§10 watched route); `CONV_S03_003` → a **status note** that its channel↔axis map is now equivariance-forced (a presentation) — the **ambient/`O_h` license (§2(1),(4)) that `LEM_S03_005/006/009`, `THM_S03_007/010/024` consume is UNCHANGED** (architect Finding 6). Each is its own `--amend` gauntlet (the η-bundle precedent). **RSM call at the gate:** retire `CONV_S03_003`'s channel↔axis load-bearing status vs a fuller re-grounding.

**(vi) Downstream (named-forward).** With the seam forced: edge routing is grounded ⟹ `b = η·a` (the edge-coupling ratio) ⟹ the **routed Now readout** `η·S_flow = 2√2/3` is forced (`LEM_010_015`). **[RECONCILED 2026-06-24 — OI-069, paired with `LEM_011_008` (RSM gate):** the prior clause "`Ψ = η·S_flow` is FORCED ⟹ Koide √2 FORCED" is **WITHDRAWN/superseded.** `η·S_flow` is the **routed Now readout / routing-FACE content** (`LEM_010_015`), **NOT** the active Ψ glyph value: the C4-mid `Ψ` is typed (`LEM_011_008`) as a **two-faced readout carrier** (routing/η **and** conjugate-support `m_base`; `|Ψ|` reads both faces, un-assignable from one alone — `LEM_011_005`), with `|Ψ|` and the Koide rendering **deferred to the Λ/G co-determination** (OI-052/OI-063; RO-G.2 gated). The `Ψ = η·S_flow` identification was withdrawn by RSM 2026-06-23 (`DEF_011_001`). Prose-only; THM_004_021's seam-forcing result (§2) is **unchanged**.]** Built elsewhere (the C4-mid `Ψ` card, `LEM_011_008`); named here (P11).

**(vii) Primitive-violation sweep (CLAUDE.md §8) — clean.** The transfer is A≠B with q ∈ 𝒟+ (strictly positive); σ = orientation × magnitude (no sign, `GUARD_001_005`); registers are units I=1 (no zero, TD-040); no metric/length (pre-metric, η a ratio); no power law (η unchanged); no 12=3·4-as-input; no physical/spacetime reading (gated). No new glyph (`G₃`/`O_h`, no "B₃"); √2/π symbolic (P8).
