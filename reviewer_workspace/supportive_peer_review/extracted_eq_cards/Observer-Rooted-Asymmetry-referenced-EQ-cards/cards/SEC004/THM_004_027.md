---
id: THM_004_027
publication_dependencies: ["DEF_004_001","LEM_004_006","LEM_004_008","LEM_004_025","THM_004_007","THM_004_021"]
---

# Forced FCC/RD from the transfer edge — the two-active/one-idle transfer orbit IS the S_12 shell (LEM_004_006), its integer span IS Λ_FCC (LEM_004_025), and the RD/Voronoi cell is INHERITED (THM_S03_007/LEM_S03_008); FCC/RD forced by transfer structure, NOT by DEF_S03_004's 12-port hypothesis or THM_S03_024's P3 minimal-norm selector

## 0) Governance & Scope Lock

**THM card (forced-shell cleanup, Option A) — the live S03 FCC/RD forcing route.** The FCC/RD geometry is forced **without** the old P3 minimal-norm selection fiat. A transfer edge is exactly **two-active / one-idle** (`THM_004_021`: `A≠B` = 2 registers, channels=registers; a 1-channel object is not an edge, the 3-channel object is the cycle). Under the coordinate presentation (`DEF_004_001`), the unique size-12 `O_h`-orbit with that structure is the `(a,a,0)`-type (`LEM_004_006`) = `S_12` at `a=1`; its integer span is `Λ_FCC` (`LEM_004_025`, parity — no minimal norm); and the cell built from that shell is the rhombic dodecahedron, **inherited** from `THM_004_007` (12-facet RD cell) / `LEM_004_008` (Voronoi). PRE-METRIC (TD-019). No new glyph. η's value is owned by `THM_004_019`.

Banned: consuming `LEM_004_005` as the shell selector, or `THM_004_009` / `DEF_S03_004`'s 12-port hypothesis / `THM_S03_024`'s P3 fiat as ground; re-proving RD/Voronoi cell geometry (inherited); re-deriving η's value (`THM_004_019`); `12 = 3·4` as an input count; metric/length/packing; port-to-face convention; signed/σ-arithmetic/zero.

## 1) Plain language

A cross-cell edge is a transfer, and a transfer needs a source and a target — **two distinct channels**, one idle (`THM_004_021`). With an orientation on each active channel, the complete support type is the two-active/one-idle orbit; counting it gives twelve. In the coordinate presentation that twelve-member orbit is exactly the `(±1,±1,0)`-type shell `S_12` — because twelve is the size of only one `O_h`-orbit type, `(a,a,0)` (`LEM_004_006`), so equal magnitude on the two active channels is forced by the orbit **size**, not by choosing the shortest vectors. The integer span of that shell is the FCC lattice (`LEM_004_025`, a parity fact), and its cell is the rhombic dodecahedron (`THM_004_007`/`LEM_004_008`). Nothing here selects "nearest direction"; the FCC-over-BCC choice is made by the transfer edge being two-channel, not three.

## 2) Statement

At the S03 presentation layer:

1. A transfer edge couples exactly two of the three channels and leaves one channel idle (`THM_004_021`); the 3-active object is the closure/cycle, not an edge.
2. The complete oriented two-active/one-idle support is a **twelve-member** orbit (3 idle-choices × 2² orientations).
3. Under the coordinate presentation, the unique size-12 `O_h`-orbit type is `(a,a,0)` (`LEM_004_006`); at `a=1` this orbit **is** `S_12 = { signed coordinate permutations of (1,1,0) }`.
4. The integer span of the forced shell is `span_ℤ S_12 = Λ_FCC` (`LEM_004_025`, norm-free parity).
5. The cell of the forced lattice is the rhombic dodecahedron: the 12-facet RD cell `C_u` (`THM_004_007`), with `C_{1/2}` the Voronoi cell of `Λ_FCC` (`LEM_004_008`) — **inherited, not re-derived.**
6. Therefore the FCC lattice and RD cell are the forced presentation of the transfer-edge structure — the FCC-over-BCC selection made by the two-channel edge (the 3-active/(a,a,a) size-8 BCC class is the cycle, excluded), **not** by minimal-norm selection.

η = √2 is the edge-invariant of this forced shell; its **value** is owned by `THM_004_019` (this theorem forces the route, names η's meaning, and does not re-derive its value).

## 3) Input contract (GR-1)

**Consumed (6 edges):** the transfer edge = 2 of c=3 channels, 1 idle; 3-active = cycle ← `THM_004_021`; the coordinate grammar / `O_h` / zero-as-absence ← `DEF_004_001`; the unique size-12 `(a,a,0)`-orbit = `S_12` ← `LEM_004_006`; `span_ℤ S_12 = Λ_FCC` (norm-free) ← `LEM_004_025`; the 12-facet RD cell ← `THM_004_007`; the Voronoi identity ← `LEM_004_008`. **Named (NOT input):** `LEM_004_005` (descriptive minimal-norm/η trace — NOT the selector); `THM_004_019` (η value); `DEF_S03_004`/`THM_S03_024` (retired, not consumed); `DEF_004_028` (port role, forthcoming). **Domains:** relational, pre-metric; finite orbit (exact integers) + parity span. **No minimal-norm selection; no superlattice uniqueness; no 12=3·4 input; no η-value derivation; no cell re-proof; no metric/packing.**

## 4) Proof

**Step 1 (two-active edge).** By `THM_004_021`, a cross-cell edge is a transfer `T(A→B)` with `A≠B` = 2 registers = 2 channels (channels=registers), leaving 1 idle; a 1-channel object is not a transfer (no target), and the 3-channel object is the closure/cycle. So the edge support is exactly two-active/one-idle.

**Step 2 (census = 12).** With an orientation (σ, `DEF_002_002` via `THM_004_021`) on each active channel, the oriented two-active/one-idle supports number `3 (idle choices) × 2² (orientations) = 12`. This is the CENSUS of the already-forced support type, never a `12 = 3·4` premise.

**Step 3 (shell = S_12, orbit-size forced).** By `LEM_004_006`, the unique size-12 `O_h`-orbit type is `(a,a,0)` (the size list `{6,8,12,24,24,48}` hits 12 for exactly one type). So a size-12 orbit cannot be `(a,b,0)` (size 24) — equal magnitude on the two active channels is forced by orbit **size**. At `a=1` this orbit is `S_12`. The coordinate `0` is display absence (`DEF_004_001`), not a zero quantity. No minimal-norm selection is used.

**Step 4 (lattice = Λ_FCC, norm-free).** By `LEM_004_025`, `span_ℤ S_12 = Λ_FCC` — a parity/linear-algebra identity, not a superlattice-exclusion.

**Step 5 (cell = RD, inherited).** By `THM_004_007`, the half-space cell determined by the 12 `S_12` directions is the rhombic dodecahedron `C_u` (12 facets); by `LEM_004_008`, `C_{1/2}` is the Voronoi cell of `Λ_FCC`. These are consumed as the cell of the already-forced lattice — inherited, not re-derived.

Therefore the FCC lattice and RD cell are the forced presentation of the two-active/one-idle transfer-edge structure. ∎

## 5) BCC / three-active guard (the F-S03fcc-1 falsifier, in the body)

The `(a,a,a)`-type orbit — the BCC nearest-neighbour class — has **eight** elements and is **three-active**. By `THM_004_021`, a three-channel object is the closure/cycle, **not** a transfer edge. So the size-8 BCC candidate is excluded **by transfer typing** (an edge is two-channel), not by a minimal-norm fiat. This is the content of falsifier **F-S03fcc-1**: were a 3-active orbit admissible as an edge, FCC-over-BCC would not be forced.

## 6) Scope & non-overclaim

This theorem forces the S03 shell/lattice/cell **route** from transfer structure. It does NOT consume `DEF_S03_004`'s port hypothesis, `THM_S03_024`'s P3 minimal-norm selector, `THM_004_009`, or `LEM_004_005` (which remains DESCRIPTIVE for the minimal-norm/η trace elsewhere — Option A). It does NOT re-prove the RD/Voronoi cell geometry (inherited from `THM_004_007`/`LEM_004_008`). It does NOT re-derive η's value (`η = √2` owned by `THM_004_019`). It does NOT define ports (that is `DEF_004_028`), claim superlattice uniqueness, use minimal-norm selection, or promote the presentation to physical background space.
