---
id: THM_004_019
publication_dependencies: ["LEM_004_005","THM_004_010"]
---

# The FCC/RD edge η = √2 (FORCED) — the C3-top edge, as c is the C2-top count: η is the minimal nonzero norm of the forced FCC lattice (THM_004_009 + LEM_004_005); D and Ω are the cell's geometry shares CARRYING the edge — the √2 in the cell volume (THM_004_010), and hence in D and Ω, IS the edge η — NOT independent constants (the DEF_003_015 idiom for c, E, G). NO power laws, ALL COUNTING; D = π/(3√2), Ω = 1 − π/(3√2) FORCED and UNCHANGED. DERIVED, not assigned

## 0) Governance & Scope Lock

**THM card (FORCED value-theorem) — the FCC/RD edge η = √2; the C3-top edge, worded as `c` is the C2-top count (`DEF_003_015`).** Forces `η = √2` (the minimal nonzero norm of the **forced** FCC lattice — `THM_004_009` + `LEM_004_005`). Just as the C2 column reads *"c is a count; E, G are channel-distribution images and the closure entry of column 1, not three independent constants"* (`DEF_003_015`), the C3 column reads: **η is the edge; D, Ω are the cell's geometry shares carrying the edge — not independent constants** — the √2 in the cell volume (`THM_004_010`), and hence in `D` and `Ω`, **IS the edge η**.

- **η := the FCC/RD nearest-neighbor edge** = the minimal nonzero norm of `Λ_FCC`; **η = √2, FORCED** (`THM_004_009`: Λ = Λ_FCC; `LEM_004_005`: minimal nonzero squared norm 2 — no other value admissible). **Derived (by counting the shell), never assigned** — as `c` is the count from `THM_003_012`.
- **D, Ω carry the edge (the forced identification — NOT a functional law):** the minimal-norm √2 **is** the √2 in the cell geometry — `d = (a/2)·√2` (`THM_004_010` Lemma B), `r = a/(2√2)`, `Vol = 4√2 r³`. So the cell-volume √2 **is** η; and since `D = π/(3√2)` (`THM_004_011`) and `Ω = 1 − π/(3√2)` (`DEF_004_012`) carry that √2, **the √2 in D and Ω is the same edge η** — exactly as `E = m·Cap` and `G` carry the count `c` (not independent constants, `DEF_003_015`).
- **NO power laws — all counting; D, Ω forced-unchanged.** As the C2 `c²` is *"two applications of across-three-channels, NOT a squaring"* (`DEF_003_014`/TD-019, a count not a power), the C3 edge is the lattice's counted minimal norm — **no `D = f(η)` power law**. D and η are both **fixed forced constants**; `π/(3η)`, `2π/(3η³)` all equal `π/(3√2)`, none is "the" forced law. `D = π/(3√2)`, `Ω = 1 − π/(3√2)` are **forced and UNCHANGED** (survived all verifications). The η-cube that briefly appeared is the **spatial volume-cube** (length³), **NOT** the c=3 connectivity (distinct 3s — `GUARD_003_009`/`GUARD_004_017`) — dropped.
- **The C3-top role:** η is the column's top edge the way `c` is the column's top count (`DEF_003_015`: the unit/connectivity row); the misfit `Γ = E/D` (a derived quotient, interpretation OPEN — `DEF_004_013`) is demoted off the top slot. **Option B:** `Γ`/`ROLEDEF_004_015` **UNTOUCHED**; the M_cell swap `Γ → η` is the **separate** `DEF_004_020` amendment; **`D`/`Ω`/`Vol` cards UNCHANGED**.

Banned: η **assigned**; any **η-power functional law** `D = f(η)` (malformed) or **value change**; a **wave/cosine/power** substrate (η is the DISCRETE edge; Koide `b/a = √2` is the aperture rendering, forward/gated — R1/R3/TD-003); a **physical length/metric/pressure** (`GUARD_004_014`); **overwriting Γ** (P9/Option B); a **zero/negative**.

## 1) Plain language

The geometry column is where the lattice first enters, and the *new* thing it brings is one number: the length of the shortest bond, proved (`LEM_004_005`) to be **√2** — the `(±1,±1,0)` "two-channels-coupled, one-idle" edge. Call it **η**. It is **forced**: the shortest bond can be nothing else.

This is worded exactly as the second column is. There, `c` is *a count*, and `E`, `G` are not new mysteries — `E` is just `m` spread across the channels, `G` is the closure (`DEF_003_015`: "not three independent constants"). Here the same way: **η is the edge**, and `D`, `Ω` are not new constants — they are the cell's geometry **shares**, the filled part and the void part, and the √2 in both **is that edge**. The √2 you already see in the cell volume, in the packing fraction `D = π/(3√2)`, and in the void `Ω = 1 − π/(3√2)` **is the very same edge √2** (the cell volume's √2 is the minimal bond, `THM_004_010` Lemma B). So `D`, `Ω` **carry the edge η** — as `E`, `G` carry the count `c`.

What we do **not** do is write `D` as a *function* of η (`D = π/(3η)`, or `2π/(3η³)`). `D` and η are both fixed numbers; "how does `D` vary with η" has no answer (there's no family — FCC is one lattice), so any such formula is a renotation, not a forced law. (And the cube that showed up was just "volume = length³" — three spatial dimensions — *not* the three connectivity channels; those are different 3s.) `D = π/(3√2)` was always forced and stays exactly as it is. This card forces **η = √2** and identifies the column's √2 as that edge — nothing more, nothing changed.

The old top entry `Γ = E/D` was a *quotient* built from other entries (meaning left open) — a derived thing in the top (edge) seat; it steps down to a derived auxiliary, left exactly as it was. η is the **discrete** edge of a pixelated lattice; the smooth Koide "wave ratio" `b/a = √2` is the *rendered* picture of this same √2 — a later, gated reading, never the substrate here.

## 2) Statement

> **(THM_004_019 — η = √2 forced; the C3-top edge, as c is the C2-top count.)** The geometry lattice is the forced FCC lattice `Λ_FCC` (`THM_004_009`).
>
> - **(1 — the edge, FORCED.)** Define **η := the FCC/RD nearest-neighbor edge invariant** = the minimal nonzero norm of `Λ_FCC`. By `LEM_004_005` (minimal nonzero squared norm 2, realized exactly by the 12 `(±1,±1,0)`-type vectors `S_12`),
> $$\boxed{\;\eta = \sqrt2\;}\quad\text{— FORCED (no other value admissible), not assigned.}$$
> - **(2 — D, Ω carry the edge; FORCED identification, NO power law.)** The minimal-norm √2 **is** the √2 in the cell geometry: `d = (a/2)·√2` (`THM_004_010` Lemma B), `r = a/(2√2)`, `Vol = 4√2 r³`. Hence **the cell-volume √2 is η**; and since
> $$D = \frac{\mathrm{Vol(ball)}}{\mathrm{Vol(cell)}} = \frac{\pi}{3\sqrt2}\ (\text{THM\_S03\_013}),\qquad \Omega = 1 - \frac{\pi}{3\sqrt2}\ (\text{DEF\_S03\_014})$$
> carry that cell-volume √2, **the √2 in D and Ω is this same edge η** — as `E`, `G` carry the count `c` (`DEF_003_015`: "not independent constants"). `D`, `Ω` are **FORCED and UNCHANGED**; this card asserts **no** `D = f(η)` (malformed — D, η both fixed) and **no** value change.
> - **(3 — the edge as top entry, as c in C2.)** η is the C3-top **edge** the way `c` is the C2-top **count** (`DEF_003_015`, the unit/connectivity row); `D`, `Ω` are the cell's geometry shares **carrying** it — an identification by counting (`THM_004_010` Lemma B), **not** a power-law.
>
> **The C3-top role:** η replaces the misfit `Γ = E/D` (`DEF_004_013`, interpretation OPEN) as the top-row **edge** — as `c` is the top-row count (`DEF_003_015`) — (the `DEF_004_020` amendment). **Option B:** `Γ`/`ROLEDEF_004_015` UNTOUCHED; `D`/`Ω`/`Vol` cards UNCHANGED. The Koide `b/a = √2` is the aperture rendering (forward/gated). √2 symbolic (P8).

## 3) Input contract (GR-1)

**Consumed (2 edges):** the minimal nonzero norm √2 / the edge shell `S_12` ← `LEM_004_005`; the cell volume `Vol = 4√2 r³` + Lemma B `d = (a/2)·√2` (the trace that the cell-volume √2 IS the minimal norm) ← `THM_004_010`. **Named (NOT a dependency):** `THM_004_009` (FCC forced — Λ = Λ_FCC; transitive via `THM_004_010`); `THM_004_011` (`D = π/(3√2)` — its √2 inherits the cell √2 = η; VALUE UNCHANGED); `DEF_004_012` (`Ω = 1 − π/(3√2)` — its √2 inherits η; VALUE UNCHANGED); `DEF_004_020` (M_cell — the top-row Γ→η swap, the substantive amendment); `THM_S03_024` (the (±1,±1,0) two-channel-diagonal ports — the 2-of-3 cross-link to `S_flow`); `DEF_004_003` (`Λ_FCC`, transitive); `DEF_004_013`/`ROLEDEF_004_015` (Γ, UNTOUCHED — Option B); `DEF_004_001` (model-internal magnitude); `GUARD_004_014` (no physical geometry); `GUARD_003_009`/`GUARD_004_017` (count discipline — cube=volume≠c=3); the Koide `b/a = √2` rendering (`GATE_008_013` RO-G.2, gated); `S_flow = (c−1)/c` (C4-top). **Domains:** ℤ³ / ℝ³ model-internal, exact `Q[√2, π]`. **No assigned η; no η-power functional law; no value change; no wave/cosine/power substrate; no physical length/metric/pressure; no Γ overwrite; no zero/negative.**

## 4) Proof (complete — η = √2 forced; D, Ω carry the edge by trace; no power law)

**Step 1 — the lattice is the forced FCC lattice.** By `THM_004_009`, the geometry lattice is `Λ = Λ_FCC` (forced — a lattice with minimal norm √2 and minimal shell `S_12` is `Λ_FCC`). ∎(1)

**Step 2 — η = √2, FORCED.** By `LEM_004_005` (complete enumeration), the minimal nonzero squared norm of `Λ_FCC` is exactly 2, attained precisely by the 12 `(±1,±1,0)`-type edge vectors `S_12`. Hence the nearest-neighbor (edge) norm is `√(2) = √2`. Define **η := this minimal nonzero norm**; **`η = √2` is FORCED** — no other value admissible. A forced VALUE-theorem (the `THM_004_011`/`THM_003_019` kind). ∎(2)

**Step 3 — D, Ω carry the edge: the column's √2 IS η (forced identification, NO power law).** By `THM_004_010` Lemma B, the minimal distance `d = (a/2)·√2` is derived **from the minimal norm √2** — so the √2 in `d`, the inradius `r = a/(2√2)`, and the cell volume `Vol = 4√2 r³` (Clause 4) **is the minimal norm η** (not a coincidental √2). By `THM_004_011`, `D = Vol(ball)/Vol(cell) = π/(3√2)` — its √2 is the cell-volume √2, hence **η**. By `DEF_004_012`, `Ω = 1 − π/(3√2)` — its √2 likewise **η**. So `D` and `Ω` **carry the single edge η** — exactly as `E = m·Cap` and `G` carry the count `c` (`DEF_003_015`: "not independent constants"). This is an **identification by trace** (the √2 in `D`, `Ω` IS the edge η, carried), **forced** by `THM_004_010` Lemma B + `η = √2`. It is **NOT** a functional law: `D` and η are both fixed forced constants, so `D = f(η)` is malformed (`π/(3η)`, `2π/(3η³)` all equal `π/(3√2)`; none is forced). `D = π/(3√2)`, `Ω = 1 − π/(3√2)` are **FORCED and UNCHANGED** (`THM_004_011`/`DEF_004_012`, survived all verifications); this card changes no value. ∎(3)

**Step 4 — η is the top edge (as c is the top count); the cube is volume, not c=3.** η is the C3-top **edge** the way `c` is the C2-top **count** (`DEF_003_015`, the unit/connectivity row); `D`, `Ω` carry it. Any η-cube in the geometry (e.g. `Vol ∝ length³`) is the **spatial volume-cube** (three spatial dimensions, `length³`), **NOT** the c=3 connectivity — distinct 3s (`GUARD_003_009`/`GUARD_004_017` count discipline); and this card writes no η-power law, so no cube appears here. ∎(4)

**What this proof does NOT do / claim:** no **assignment** of η; no **functional law** `D = f(η)` (malformed — D, η both fixed) and no **value change** (D, Ω forced-unchanged); no conflation of the volume-cube with c=3 (`GUARD_003_009`/`GUARD_004_017`); no **wave/cosine/power** substrate (η is the discrete edge; `b/a = √2` is the aperture rendering, forward); no **physical length/metric/packing-pressure** (`GUARD_004_014`); no **Γ overwrite** (`DEF_004_013`/`ROLEDEF_004_015` untouched — Option B; the M_cell swap is the `DEF_004_020` amendment); no edit of any promoted card except (in the bundle) `DEF_004_020`.

## 5) Scope & non-overclaim

Establishes: **η = √2** as the FORCED FCC/RD edge (the minimal nonzero norm of the forced `Λ_FCC` — `THM_004_009` + `LEM_004_005`), the C3-top **edge as `c` is the C2-top count** (`DEF_003_015`), **carried by `D`, `Ω`** — the column's √2 (cell volume `THM_004_010`, and hence `D`, `Ω`) **is** this edge η (identification by trace, every step forced). Does NOT: **assign** η; assert any **η-power functional law** `D = f(η)` (malformed — D, η fixed) or **change any value** (D = π/(3√2), Ω = 1 − π/(3√2) UNCHANGED, forced upstream); conflate the spatial volume-cube with c=3 (`GUARD_003_009`/`GUARD_004_017`); build a **wave/cosine/power** substrate or assert the Koide `b/a = √2` as anything but the gated aperture rendering (R1/R3/TD-003); claim a **physical** length/metric/pressure/optimality (`GUARD_004_014`); **edit/retire Γ** (`DEF_004_013`/`ROLEDEF_004_015` UNTOUCHED — Option B) or amend `D`/`Ω`/`Vol` (UNCHANGED; only `DEF_004_020` M_cell is amended); admit a **zero/negative**. Guard references: `THM_004_009`/`LEM_004_005` (the forced lattice/edge), `THM_004_010` Lemma B (the trace), `DEF_004_001`/`GUARD_004_014` (model-internal, no physical reading), `GUARD_003_009`/`GUARD_004_017` (count discipline), `GUARD_004_004`. The M_cell swap, the Koide rendering, and `S_flow` (C4) are named reference (P11), not built here.

## 6) Per-symbol accounting

**Introduced: η** (the FCC/RD edge invariant; `symbols_introduced: [η]`; R17 collision audit in frontmatter — distinct from `ε`, `ε₄`, `h`, `ħ`, `Γ`; registry held no prior η). **Consumed as ground (2 edges):** the minimal norm √2 / `S_12` ← `LEM_004_005`; the cell volume + Lemma B trace ← `THM_004_010`. **Registry symbols used:** `η` (introduced), `Λ_FCC` (`DEF_004_003`, via deps), `S_12` (`LEM_004_005`). **Named (NOT consumed; VALUES UNCHANGED):** `THM_004_009` (forced FCC), `THM_004_011` (`D = π/(3√2)`), `DEF_004_012` (`Ω = 1 − π/(3√2)`), `DEF_004_020` (M_cell, the amendment), `THM_S03_024`, `DEF_004_013`/`ROLEDEF_004_015` (Γ, untouched), `DEF_004_001`, `GUARD_004_014`, `GUARD_003_009`/`GUARD_004_017`, the Koide `b/a` rendering, `S_flow`, `π` (symbolic — in the named D, Ω, not introduced here). **Undeclared: none. No decimal except a [COMPARISON OUTPUT]-marked pairing (P8); no physical/length/metric symbol; no η-power law; no zero.**

## 7) Verification (witness SPEC — for the independent witness author; P12: mode EXACT-SYMBOLIC, Q[√2, π]; reads live promoted ground, TD-030; GENUINE checks, NO hardcoded booleans, F-256; TD-035 disjoint fields; run lint_witnesses.py)

Witness (exact-symbolic; reads the REAL `LEM_004_005`/`THM_004_010` ground LIVE; modes A–E each flip exactly one check):

1. **η = √2 FORCED, computed not assigned (Mode A teeth, LOAD-BEARING — F-S03η-1).** COMPUTE the minimal nonzero norm from `LEM_004_005`'s lattice (enumerate `Λ_FCC` / read the proved minimal shell `S_12`) and assert `η = √2` exactly. Mode A: corrupt the lattice's minimal norm (inject a squared-norm-1 vector, or set the shell norm to √3) ⟹ η moves ⟹ check 1 fails.
2. **D, Ω carry the edge: the cell-volume √2 IS the minimal norm η (Mode B teeth, LOAD-BEARING — F-S03η-2).** TRACE `THM_004_010` Lemma B (`d = (a/2)·√2` → `r = a/(2√2)` → `Vol = 4√2 r³`) and confirm the cell-volume √2 descends from the minimal norm; hence the √2 in `D = π/(3√2)` and `Ω = 1 − π/(3√2)` (which inherit it) is η. Mode B: break the trace (the volume √2 not the minimal norm) ⟹ check 2 fails.
3. **NO η-power law / values unchanged (Mode C teeth, LOAD-BEARING — F-S03η-3, the corrected fence).** Assert the card writes NO functional `D = f(η)` (no `pi/(3*eta)`, no `pi/(3*eta**3)`, no `eta`-power equation for D/Ω/Vol) AND `D == π/(3√2)`, `Ω == 1 − π/(3√2)` (values unchanged). Mode C: manufacture an η-power law / change a value ⟹ check 3 fails.
4. **No wave/power/physical leak (Mode D — F-S03η-4).** Assert η is the discrete lattice edge: `is_wave_amplitude == False`, `is_power_substrate == False`, `is_physical_length == False`; the Koide `b/a = √2` named-forward (gated). Mode D: type η as a wave amplitude / power / physical length ⟹ check 4 fails.
5. **Option B / no Γ overwrite / bundle (Mode E — F-S03η-5).** Assert `DEF_004_013` (Γ)/`ROLEDEF_004_015` resolve UNEDITED (byte-match HEAD); η a fresh glyph; and the amendment bundle is `{DEF_004_020}` only (D/Ω/Vol cards UNCHANGED). Mode E: edit/retire Γ, reuse an occupied glyph, or amend a D/Ω/Vol card ⟹ check 5 fails.

6. **Can-fail (--corrupt), REQUIRED — bare `--corrupt` → the value Mode A; each mode flips ONE check (TD-035).** A→1 … E→5. Plus card-resolution (THM_004_019 + its 2 deps, eq_spine-first). **Run the full mode×check matrix; assert the bijection A→1…E→5; assert normal = 0.** Modes A/B read live promoted ground (TD-030). Run `lint_witnesses.py`; `last_pass_hash: null`; run `promote_card.py --dry-run` as the final pre-gate check.

A pass discharges the structure (η = √2 forced; the column's √2 IS η by trace; NO η-power law, D/Ω values unchanged; discrete edge, no wave/physical; Γ untouched; bundle = M_cell only). It changes no value and edits no promoted card. Human-gated at the RSM gate.

## 8) Falsifiers

As frontmatter — **F-S03η-1** (η ≠ √2 / assigned — Mode A, LB, the value), **F-S03η-2** (the column's √2 not the edge η / threading fails — Mode B, LB, the identification), **F-S03η-3** (η-power law manufactured / value changed — Mode C, LB, the corrected fence), **F-S03η-4** (wave/power/physical leak — Mode D), **F-S03η-5** (Γ overwritten / collision / bundle — Mode E). A THM with constitutive falsifiers (D-025.R25): the value can lose (if the minimal norm is not √2) and the threading can lose (if the column's √2 does not descend from the minimal norm). Loss handling per D-025.R26/D-042: the consumed promoted facts (`LEM_004_005`/`THM_004_010`) hold; a violation diagnoses THIS card (a mis-identified edge, a broken trace, a manufactured law), never a foundational law. **Reopen:** an RSM re-ruling on the C3-top, or a change to the FCC/RD geometry upstream.

## 9) Notes

**(i) Derive, not assign (the FORCED value).** η = √2 is the minimal nonzero norm of the forced `Λ_FCC` (`THM_004_009` + `LEM_004_005`, exhaustive enumeration) — a lattice invariant, no other value admissible. The witness COMPUTES it (corrupting the lattice moves it), never hardcodes √2 (TD-030).

**(ii) The threading is an IDENTIFICATION, not a functional law (RSM 2026-06-22 correction).** D and η are both **fixed forced constants**, so "D as a function of η" has no canonical forced form — `π/(3η)`, `2π/(3η³)`, `πη/6` all equal `π/(3√2)`, and none is "the" forced law (there is no one-parameter family; FCC is one lattice). The honest forced relationship is: **the √2 in the cell volume, in D, and in Ω IS the edge invariant η** — by the trace `THM_004_010` Lemma B (minimal norm → d → r → Vol). `D`, `Ω` **carry the edge**, as `E`, `G` carry the count `c` (`DEF_003_015`: "not independent constants"). `D = π/(3√2)`, `Ω = 1 − π/(3√2)` are **forced and UNCHANGED** (they survived all verifications; nothing about their forcedness changed — only the malformed 'function of η' goal was dropped).

**(iii) The cube was volume, not c=3.** Any η-cube in the geometry is the spatial **volume**-cube (`length³`, three spatial dimensions), NOT the c=3 connectivity — distinct 3s (`GUARD_003_009`: channel-count ≠ spatial dimension; `GUARD_004_017`). This card writes no η-power law, so no cube appears; the note records the discipline.

**(iv) The edge carried by D, Ω; no cycle.** η is introduced from the edge (`LEM_004_005`) and the cell-volume trace (`THM_004_010`); `D`/`Ω`/`Vol` are NOT amended (their √2 is η by this card's identification — they *carry* the edge, as E, G carry c), so there is no DAG cycle and the bundle shrinks to `{DEF_004_020 M_cell Γ→η + register η}`. The DAG: `THM_004_019 → {LEM_004_005, THM_004_010}`; `DEF_004_020 → THM_004_019` — acyclic.

**(v) The rendering fence (R1/R3/TD-003).** η = √2 is the DISCRETE FCC edge. The continuous Koide wave ratio `b/a = √2` is the APERTURE RENDERING of this same √2 (forward, gated RO-G.2 / `GATE_008_013`) — the old Koide work survives there as the rendered form, never as the substrate. No `a + b·cos`, no `(Σ√m)²` power-as-substrate on this card.

**(vi) The 2-of-3 cross-link (named, not load-bearing).** The edge is the `(±1,±1,0)` "two channels coupled, one idle" bond (`THM_S03_024`) — the same 2-of-3 structure that gives the C4-top `S_flow = (c−1)/c = 2/3`. Geometry's 2-of-3 (η = √2) and the readout's 2-of-3 (S_flow = 2/3) are why the two top-row invariants line up for the Koide rendering — the C4 phase, named here only.

**(vii) Primitive-violation sweep (CLAUDE.md §8) — clean.** η = √2 > 0 (a discrete norm); D, Ω ∈ (0,1) unchanged; `D + Ω = I` transfer form, never subtraction (`DEF_004_012`); no negative/zero object; no signed arithmetic; no η-power-as-substrate; no wave/power substrate (R1/R3); no physical length/metric (TD-003); π, √2 symbolic (P8). η a fresh glyph.

**(viii) Forward / fences held.** The M_cell swap (`DEF_004_020`), the Koide `b/a` rendering (gated), and `S_flow = 2/3` (C4) stay OPEN/forward, built by no part of this card. `D = π/(3√2)` (`THM_004_011`), `Ω = 1 − π/(3√2)` (`DEF_004_012`), the cell volume (`THM_004_010`), `Γ = E/D` (`DEF_004_013`), and `ROLEDEF_004_015` are named, **never edited** (Option B; values unchanged). No value changed; no η-power law; no zero; no physical geometry.
