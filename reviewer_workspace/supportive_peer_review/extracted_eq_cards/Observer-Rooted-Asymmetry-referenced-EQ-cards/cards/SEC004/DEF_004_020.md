---
id: DEF_004_020
publication_dependencies: ["DEF_003_015","DEF_004_012","GUARD_003_006","THM_004_011","THM_004_019"]
---

# M_cell — the 3×3 cell matrix: M_base plus the geometry column [η, D, Ω] (one ladder step, D-030.Q3); the C3 top is the edge generator η (THM_004_019), as c is the C2 top count

## 0) Governance & Scope Lock

Definition card: assembles the 3×3 cell matrix from already-defined entries — ONE ladder step (M_base 3×2 → M_cell 3×3), no layer mixing (GUARD_003_006's cross-rung fence). Exact symbolic forms only. The G slot carries a ROLE, never a value; the Ω slot is a bookkeeping location, never a runtime valuation. Layer ceiling S03.

**[AMENDMENT 2026-06-22 — the C3-top generator bundle (RSM-gated; the other half of THM_004_019).]** The geometry-column TOP entry is swapped **Γ → η**: the C3-top is the **edge generator η** (= √2, FORCED — `THM_004_019`), as the C2-top is the count `c` (`DEF_003_015`). The geometry column reads **[η, D, Ω]**: η the edge; **D, Ω the cell's geometry shares CARRYING the edge** (the √2 in `D = π/(3√2)` and `Ω = 1 − π/(3√2)` **IS** η) — not independent constants (the `DEF_003_015` idiom). **Option B:** `Γ = E/D` (`DEF_004_013`) and its role (`ROLEDEF_004_015`) are **UNTOUCHED** — `Γ` stays a defined **off-matrix** derived auxiliary (the prior top entry, demoted). **D, Ω values UNCHANGED**; the `[1,3]` entry changes `Γ = 27√2/(2π²)` → `η = √2`; the prior `Γ·D = E` identity leaves the matrix with `Γ` (it remains a property of the off-matrix `Γ = E/D`). **NO power laws — all counting** (`THM_004_019`).

## 1) Plain language

The book's third column is the geometry column, and the cell matrix is born: three rows, three columns. The first two columns are the base book unchanged. The new column's **top is the edge** the FCC/RD geometry introduces — **η = √2**, the shortest bond, forced by counting the lattice (`THM_004_019`) — exactly as the second column's top is the channel **count** `c`. Below it sit **D** (the filled share) and **Ω** (the void share), and — the careful point — the √2 in both **is that same edge η**: D and Ω **carry** the edge, just as `E` and `G` carry the count `c` (`DEF_003_015`: "not three independent constants"). Read the rows: the unit/edge row `[I, c, η]`; the cycle row `[τ, E, D]`; the bottom (conjugacy) row `[m, G, Ω]` — density, closure, residual. Three columns, zero new mysteries: each new entry is the edge, or a share carrying it.

*(What changed in this amendment: the old top entry was `Γ = E/D`, a derived quotient with its meaning left open — a generated thing in a generator's seat. It steps down to a defined off-matrix auxiliary (its card `DEF_004_013` and role `ROLEDEF_004_015` untouched); the edge η takes the top. No value of D or Ω changes; no power law is written — the √2 is the counted edge, not a function of anything.)*

## 2) Statement

$$M_{\text{cell}} \;=\; \begin{pmatrix} I & c & \eta \\ \tau & E & D \\ m & G & \Omega \end{pmatrix}
\;=\; \begin{pmatrix} 1 & 3 & \sqrt2 \\[1ex] 2\pi & \dfrac{9}{2\pi} & \dfrac{\pi}{3\sqrt2} \\[1ex] \dfrac{1}{2\pi} & \text{(role)} & 1 - \dfrac{\pi}{3\sqrt2} \end{pmatrix}$$

**One ladder step (D-023.1/4, D-030.Q3):** columns 1–2 = $M_{\text{base}}$ (`DEF_003_015`, unchanged, entry-for-entry); column 3 = the geometry column $[\eta, D, \Omega]^{\mathsf T}$ — **η the edge** (`THM_004_019`: η = √2, the minimal nonzero norm of `Λ_FCC`, FORCED), **D, Ω the shares carrying it** (`THM_004_011`: `D = π/(3√2)`; `DEF_004_012`: `Ω = 1 − π/(3√2)`; the √2 in each **IS** η). The G slot is displayed "(role)" — value OPEN (`ROLEDEF_003_011` via `DEF_003_015`). Row semantics: unit/edge row $[I, c, \eta]$; cycle row $[\tau, E, D]$; bottom (conjugacy) row $[m, G, \Omega]$ — density, closure, residual (D-023.1).

**The C3 column is NOT three independent constants** (the `DEF_003_015` idiom for C2): **η is the edge**; **D, Ω carry it** (the √2 in D, Ω is η); `D + Ω = I`. **NO power law** (`THM_004_019`): no functional `D = f(η)`; D, Ω VALUES unchanged. **Γ = E/D** (`DEF_004_013`) is the **demoted off-matrix** derived auxiliary (Option B; untouched).

**The Ω entry is a bookkeeping LOCATION** (D-025.R13): its runtime valuation ($\omega_{\text{cell}} = \Omega_{\text{pack}}$) arrives only through S04's explicit identity card.

## 3) Input contract (GR-1)

Objects: $M_{\text{cell}}$ (3×3 array of typed entries — each typed at its own card before this assembly, TD-009); $M_{\text{base}}$ ← `DEF_003_015`; **η ← `THM_004_019`** (the edge = √2, FORCED); D ← `THM_004_011`; Ω ← `DEF_004_012`; ladder/mixing discipline ← `GUARD_003_006` (cross-rung fence; E-LADDER enforces shape). Domains: exact symbolic forms plus the count c, the edge η, and the role slot G (heterogeneous assembly, declared — the `DEF_003_015` pattern). **Named (NOT a dependency): Γ = E/D (`DEF_004_013`) — the demoted off-matrix auxiliary, untouched (Option B); `ROLEDEF_004_015` (Γ's role, untouched).** Quantifiers: none. Finite-N surface: the §7 assembly checks. Limit mode: none. **No power law; no value change; no Γ in the matrix; no spacetime/wave reading.**

## 4) Justification (DEF obligation: well-formedness + presentation honesty)

- **Non-emptiness:** all nine slots bound to existing formal objects; exactly one slot (G) bound to a role (D-026.Q4). The C3-top is η (`THM_004_019`, promoted).
- **One-step discipline:** exactly one column is added relative to `DEF_003_015`; the base block is consumed unchanged (witness-checked entry-for-entry) — no layer mixing, no M_rank content (`GUARD_003_006`'s cross-rung fence).
- **The amendment (Γ → η), value-preserving:** the geometry-column top entry changes from the derived quotient `Γ = E/D` to the **edge generator** `η = √2` (`THM_004_019`). `D` and `Ω` are **unchanged** (`THM_004_011`/`DEF_004_012`); their √2 **is** η (the edge they carry). The C3 column reads as the `DEF_003_015` idiom (edge + shares carrying it, not independent constants). **No power law** is written (`THM_004_019`: `D = f(η)` is malformed; the √2 is the counted edge). **Option B:** `Γ = E/D` stays defined (`DEF_004_013`/`ROLEDEF_004_015` untouched), off the matrix.
- **Entry-status honesty:** the assembly upgrades nothing: Ω's finalization stays pending-RSM (`DEF_004_012`), D's class stays provisional (its card), G's value stays open; η is the forced edge (`THM_004_019`); the C3 column is the edge + shares carrying it, not new primitives.
- **Presentation honesty (GAP-S02-D inherited):** WHY a matrix rather than a list remains the open S02 gap; this card extends the PRESENTATION by its ruled ladder step (now with η at the C3-top) and resolves nothing about the organization question.

## 5) Scope & non-overclaim

Assembles the 3×3 structure with η at the C3-top (the `Γ → η` amendment, value-preserving for D, Ω). Does NOT claim: a derivation of the matrix organization (GAP-S02-D open); any value for G; **a functional `D = f(η)` power law** (`THM_004_019`: malformed) or **any change to D, Ω values** (unchanged); that the C3 entries are independent constants (η the edge; D, Ω carry it — `DEF_003_015` idiom); any runtime/valuation content (S04; D-025.R13); any spacetime/operator/wave reading (`GUARD_S02_015`; the Koide `b/a = √2` is the gated aperture rendering of η, forward — `THM_004_019`); **any edit of Γ** (`DEF_004_013`/`ROLEDEF_004_015` UNTOUCHED — Γ the demoted off-matrix auxiliary, Option B). The geometry column's standing is conditional exactly where its entries are (η/D and derivates ride `DEF_S03_004`'s hypothesis through `THM_S03_010/011/012/013/028`). Guard references: `GUARD_001_006`, `GUARD_S02_015`, `GUARD_003_009`, `GUARD_004_004`, `GUARD_004_017`, `GUARD_004_014`.

## 6) Per-symbol accounting

Local: C1, C2, C3 (card-local column labels — the `DEF_003_015` precedent). Imported: M_base and columns 1–2's entries ← `DEF_003_015` (entry cards through it); **η ← `THM_004_019`** (the edge = √2); D ← `THM_004_011`; Ω ← `DEF_004_012`; ladder names ← their own matrix DEFs (`DEF_003_015` M_base, this card M_cell; the cross-rung fence `GUARD_003_006`); π ← registry (symbolic). **Named (NOT consumed): Γ = E/D ← `DEF_004_013` (the demoted off-matrix auxiliary, untouched); `ROLEDEF_004_015` (Γ's role, untouched).** M_cell is a registry symbol; this card is its defining site (the ladder meaning anticipates this shape; the C3-top entry is now η). Undeclared: none. No power law; no value change.

## 7) Verification (witness SPEC — for the witness author; Witness-Author docstring line per witness_harness/README rule 6; resolve the card eq_spine-first, rule 7)

Witness (exact-symbolic, symbolic π and √2): assemble M_cell from the entry cards' exact values and assert: (1) shape exactly 3×3; (2) columns 1–2 equal `DEF_003_015`'s M_base entry-by-entry (value-level, exact — including the role marker in the G slot); (3) **geometry column exact with η at the top: η = √2 (read live from `THM_004_019`), D = π/(3√2), Ω = 1 − π/(3√2); AND the carry/identity relations `D + Ω = I` and the √2 in D and Ω equals η (value-level — D = π/(3η), Ω = 1 − π/(3η) at η = √2, the DEF_003_015 carry-pattern)** — replacing the prior `Γ·D = E` matrix identity (Γ is off-matrix); (4) the G slot is VALUE-OPEN: reading it as a number must refuse/raise (proven fallible — corrupt mode); (5) the Ω slot carries a location-not-valuation marker (the D-025.R13 enforcement executable); (6) representation audit: symbolic π/√2 only, no decimals (P8); (7) normalization table: parse the frontmatter declaration and assert all nine entries carry the declared classes value-for-value (η: n/a forced-edge; D, Ω classes unchanged; a dropped flag is a failure); (8) **Option B / no power law: Γ = E/D (`DEF_004_013`) + `ROLEDEF_004_015` resolve in eq_spine AND byte-match git HEAD (Γ untouched, off-matrix); the matrix carries NO functional `D = f(η)` and NO changed D/Ω value; η resolves live from `THM_004_019`.** Can-fail (--corrupt): populate the G slot with 27π/2 in memory; assertion (4) MUST fail. (The amended witness will carry disjoint can-fail modes for the new checks 3/8 per TD-035; authored independently, P12.)

## 8) Falsifiers

Not applicable (DEF, assembly card). Reopen conditions: any consumed entry's card reopening (the assembly re-checks); `THM_004_019` reopening (the η edge); a GAP-S02-D structure derivation landing; a failed Q1 confirmation at the gate (`DEF_004_012`'s reopen clause cascades here).

## 9) Notes

**(i) The amendment (Γ → η), value-preserving.** The geometry-column top entry changes from the derived quotient `Γ = E/D = 27√2/(2π²)` to the edge generator `η = √2` (`THM_004_019`, promoted; F-278). `D = π/(3√2)`, `Ω = 1 − π/(3√2)` are UNCHANGED; their √2 IS η (the edge they carry). The C3 column now reads as the C2 column does (`DEF_003_015`): a top primitive (η the edge / c the count) + entries carrying it (D, Ω / E, G), "not independent constants."

**(ii) Option B — Γ untouched, demoted off-matrix.** `Γ = E/D` (`DEF_004_013`) and its role `ROLEDEF_004_015` are UNTOUCHED — `Γ` remains a defined derived auxiliary (the closure-load per accepted density), now off the M_cell top slot. The prior `Γ·D = E` cross-entry identity is a property of the off-matrix `Γ`, no longer an M_cell entry-relation.

**(iii) No power law — all counting (`THM_004_019`).** η is the counted minimal edge (√2); D, Ω carry it; `D = f(η)` is malformed (`π/(3η)`, `2π/(3η³)` are renotations equal only at η²=2). The η-cube anywhere is the spatial volume-cube (length³), NOT c=3 (`GUARD_003_009`/`GUARD_004_017`). The Koide `b/a = √2` is the gated aperture rendering of η (forward — `THM_004_019`), never a wave/power substrate here.

**(iv) Forward pointers only (TD-020):** S04 adds the runtime column (M_rank 3×4 — `THM_003_016`'s forced axis; content delivered there); S04's identity card values the Ω slot (D-025.R13); S06's regime table reads the completed geometry column through its own cards. The bottom-row reading (density, closure, residual) is ruled content (D-023.1) whose rule-form theorem lives at `THM_003_018 (formerly THM_S02_023, retired D-064)`. The C4 top (`S_flow`, the invariant flow of time) is the next phase, named here only.
