---
id: DEF_003_002
publication_dependencies: ["AX_001_001","DEF_003_001","THM_002_014"]
---

# τ — the S02 closure-traversal label; its value 2π is INHERITED from the FORCED t_base (THM_002_014), not a normalization convention (supersedes CONV_S02_002, D-064; non-destructive re-grounding, base-vs-active distinction preserved per D-054)

## 0) Governance & Scope Lock

Defines τ, the S02 booking label for one full closure traversal, and grounds its value
2π in the **forced** angular extent `t_base = 2π` (THM_002_014). Exact symbolic forms only —
π stays symbolic (P8). **Supersedes CONV_S02_002** (D-064): the value is no longer a declared
normalization ("nothing forces 2π") but the forced closure extent inherited from S01. Layer
ceiling S02. **Base-vs-active preserved (D-054):** τ (active S02) is NOT unified with t_base
(S01-local) — only its provenance is re-grounded.

## 1) Plain language

The book needs a name for "one full closure traversal" — the unit of going all the way
around and closing. That name is τ, and its value is 2π. Earlier this was booked as a mere
label — "we call it 2π, nothing makes it 2π." That is no longer true: the Theorem of Time
(THM_002_014) **forces** the angular extent of one orientation-preserving closure to be 2π
(a full turn subtends 2π for any radius, forced by the admission-status asymmetry — the same
directionality the whole ground rests on). So τ = 2π is **forced ground inherited from
upstream**, not a chosen normalization. τ stays the book's own working symbol (distinct from
the S01 base name t_base); what changed is only *why* its value is 2π.

## 2) Statement

**Definition (τ, the closure-traversal label):** τ is the S02 booking unit attached to the
closure objects whose records the base book retains (DEF_003_001 T2; the closure object is
formal at THM_002_011). τ is the closure-traversal (the cycle) by nature; its M_base side is service (row 2, THM_003_018).

**Value (forced, inherited):**

$$\tau \;=\; 2\pi$$

— the value is the **forced** dimensionless angular extent of one complete
orientation-preserving closure (THM_002_014: `t_base = 2π`, forced by the admission-status
asymmetry; §4 Step 4 / normalization note). τ carries that same value-shape as the S02 active
symbol; it is **not** unified with the S01-local t_base (D-054), and this card does **not**
re-derive 2π at S02 — it consumes the upstream forcing.

## 3) Input contract (GR-1)

Objects: τ (cycle label, TD-009-typed); π (exact symbolic, DEF_002_016(c)); the forced
extent `t_base = 2π` ← THM_002_014 (consumed as the value-source, not unified). Domains: the
base book's cycle-quantity class. Quantifiers: none. Finite-N surface: the exact identity
checks of §7. Limit mode: none.

## 4) Why the value is forced (supersedes the old "convention; alternatives" §4)

CONV_S02_002 typed itself CONV on the claim that "nothing at this layer forces 2π" and that
the only derivation was S05 phase-closure (unavailable at S02). Both are superseded:

- **THM_002_014 forces 2π upstream (S01).** Its normalization note: *"The 2π is FORCED (the
  dimensionless angular extent of one orientation-preserving closure), NOT chosen by
  normalization."* The forcing is the admission-status asymmetry (A admitted, ¬A the
  non-admission boundary — orientation-preserving full turn 2π, not a π-flip), the same
  directionality that grounds the whole spine.
- **The former "alternatives" are foreclosed.** `τ:=1` (unit) and `τ:=π` (half-turn)
  contradict the forced full-turn extent 2π; they are not coherent once the extent is forced
  (same illusory-alternatives pattern D-064 found at CONV_S01_009).
- **The S05 phase route stays deferred** — it may RE-derive 2π at its own layer later
  (TD-020-legal), but it is not the ground here; the S01 forcing is.

Hence DEF-derived, not CONV. The residual convention (naming the label τ, its service-side role) is
definition work.

## 5) Scope & non-overclaim

Defines the label and grounds its value in forced upstream ground. Does NOT: unify τ with
t_base (D-054 — separate wave); re-derive 2π at S02; read τ as physical/measured time
(GUARD_003_006); invoke phase/U(1) semantics (S05). Guard references: GUARD_003_006,
GUARD_001_006.

## 6) Per-symbol accounting

Local: none introduced (τ, π are registry symbols; this is τ's active S02 declaring site,
moved here from CONV_S02_002 per D-064). Imported: the forced extent value ← THM_002_014
(t_base = 2π, consumed as value-source); I ← AX_001_001 (named in m·τ = I at DEF_003_003,
not computed here); the tracked-category universe ← DEF_003_001. Undeclared: none.

## 7) Verification

Witness (exact-symbolic, symbolic π; copied/adapted from CONV_S02_002_witness.py per D-064):
(1) assert τ equals 2·π exactly (canonical-form equality, π symbolic — no floats); (2) assert
the value equals THM_002_014's forced t_base value-shape 2π (the inheritance is real, not
decorative); (3) assert each foreclosed alternative (1, π) is exactly unequal to 2π
(symbolically). Can-fail (--corrupt): corrupt the expected value to τ := π; assertion (1) MUST
fail. A pass certifies display/identity consistency + the inherited-value equality — the
forcing itself is THM_002_014's (verified there), consumed here.

## 8) Falsifiers

See frontmatter F-D002-1. The value's adequacy is consumed at DEF_003_003 (m·τ = I, already
re-grounded on THM_002_014's m_base) and its service-side role at THM_003_018 (M_base row 2).

## 9) Notes

Supersedes CONV_S02_002 (D-064, 2026-07-14). CONV_S02_002 remains materialized as
status: superseded — queryable, non-load-bearing; its promotion record, witness id, and prior
references stay valid. This is the τ-wave that D-054 deferred, taken in its **non-destructive**
form (option 1): provenance re-grounded, symbol identity unchanged (τ ≠ t_base preserved).
Full τ↔t_base unification (option 2) remains a separate wave. Cap impact: the 2π in
`E = 9/(2π)` and `G = Cap·τ = 18π` now rests on forced ground, not a convention.
