---
id: DEF_003_015
publication_dependencies: ["DEF_003_004","DEF_003_005","DEF_003_014","ROLEDEF_003_011"]
---

# M_base — the first matrix structure, 3×2: the existence column plus the constraint column [c, E, G]

## 0) Governance & Scope Lock

Definition card: assembles the first matrix structure from already-defined entries. Exact
symbolic forms only. The G slot carries a ROLE, never a value. No geometry entries exist
at this layer; the 3×3 and 3×4 ladder objects are forward pointers only. Layer ceiling
S02.

## 1) Plain language

The book's second column arrives, and with it the first genuine matrix: three rows, two
columns. The first column is the existence column from before. The second column holds
the constraint entries: the channel count c, the distribution total E, and the closing
entry G. Read the rows: the unit row [I, c] — existence and its channels; the cycle row
[τ, E] — the closure cycle and what distribution services it; the bottom row [m, G] — the
density and its closure. The second column is not three new mysteries: c is a count, E is
just m spread across the channels twice, and G is a role whose number is honestly open.

## 2) Statement

$$M_{\text{base}} = \begin{pmatrix} I & c \\ \tau & E \\ m & G \end{pmatrix}
= \begin{pmatrix} 1 & 3 \\ 2\pi & \dfrac{9}{2\pi} \\ \dfrac{1}{2\pi} & \text{(role)} \end{pmatrix}$$

**The FIRST MATRIX STRUCTURE — 3×2 by the one-column-at-a-time law** (D-023.1;
D-025.R7 revised: primitive among matrices; the fundamental object remains M_exist).
Column 1 = M_exist (DEF_003_004). Column 2 entries are **channel-distribution images and
the closure entry of column 1**, not three independent constants: c ← DEF_003_005 (the
count), E = 3·3·m ← DEF_003_014, G ← ROLEDEF_003_011 (the ROLE; value OPEN — the slot is
displayed as "(role)", never as a number).

**Bottom row [m, G]:** density and its closure. With G silent (self-coupled readout 0,
ROLEDEF_003_011), the row's live content is m = 1/τ — the continuous booking the ruled
bottom-row reading speaks of (D-023.1), whose rule-form is the residual-rule THEOREM
TARGET (D-030.Q6; §9).

## 3) Input contract (GR-1)

Objects: M_base (3×2 array of typed entries — each entry typed at its own card before
this assembly, TD-009); columns C1 = M_exist, C2 = [c, E, G]. Domains: exact symbolic
forms plus the count c and the role slot G (the slot's type is "role entry," not
"symbolic value" — the assembly is heterogeneous and says so). Quantifiers: none.
Finite-N surface: the §7 assembly checks. Limit mode: none.

## 4) Justification (DEF obligation: well-formedness + presentation honesty)

- **Non-emptiness:** all six slots are bound to existing formal objects; exactly one slot
  (G) is bound to a role rather than a value, by construction (D-026.Q4 — the value-gap
  closes only when THM_003_019 exists).
- **Entry-status honesty:** the assembly upgrades nothing — each entry keeps its own
  card's registry status; the column-2 entries remain images/role of column 1's content.
- **Presentation honesty (GAP-S02-D, open):** WHY a two-axis structure (quantity-kind ×
  role) rather than a six-entry list is NOT derived here — the atlas records the
  derivation as missing everywhere, and the gap is open new-work. At this card the matrix
  is a presentation whose axes are named (rows: the three quantity-kinds of the existence
  column; columns: existence vs constraint role); if the gap loop later derives the
  structure from P2's realization requirements, that lands as its own card depending on
  this one.
- **Ladder discipline:** the next objects (M_cell 3×3 with the geometry column at S03;
  M_rank 3×4 at S04) are forward pointers; their slots are NOT reserved with values here
  (the F-020 lesson: nothing smuggled into the matrix definition undeclared).

## 5) Scope & non-overclaim

Assembles the 3×2 structure. Does NOT claim: a derivation of the matrix organization
(GAP-S02-D open); any value for G; independence of the column-2 entries (they are images
of column 1's content); any spacetime, operator, or spectral reading (GUARD_S02_015);
any residual RULE (theorem target, D-030.Q6); anything about 3×3/3×4 content (S03/S04,
forward-flow only). Guard references: GUARD_001_006, GUARD_S02_015, GUARD_003_009.

## 6) Per-symbol accounting

Local: C1, C2 (card-local column labels). Imported: M_exist ← DEF_003_004; I, τ, m via
M_exist's entry cards; c ← DEF_003_005; E ← DEF_003_014; G (role) ← ROLEDEF_003_011; π ←
registry (symbolic). M_base is a registry symbol; this card is its defining site.
Undeclared: none.

## 7) Verification (witness SPEC — for the witness author; Witness-Author docstring line
per README rule 6; resolve card eq_spine-first, rule 7)

Witness (exact-symbolic, symbolic π): assemble M_base from the entry cards' exact values
and assert: (1) shape exactly 3×2; (2) column 1 equals M_exist's column entry-by-entry
(value-level, exact); (3) value slots exact: c = 3 (as a count), E = 9/(2π), and
E = 9·m ACROSS the assembled entries (cross-slot consistency); (4) the G slot is
VALUE-OPEN: the assembly carries a role marker, and any attempt to read the G slot as a
number must refuse/raise (the implementation enforces value-openness — checked by
attempting the read and asserting refusal); (5) bottom-row live content: m·τ = I exactly
across slots; (6) representation audit: symbolic π only, no decimals (P8). Can-fail
(--corrupt): populate the G slot with 27π/2 in memory; assertion (4) MUST fail (the
value-open enforcement is itself the thing proven fallible). All checks value-level.

## 8) Falsifiers

Not applicable (DEF, assembly card). Reopen conditions: GAP-S02-D's derivation landing
(structure card added above this one); a residual-rule THM whose content requires a
different bottom-row presentation; any consumer needing a slot this shape cannot host —
each returns through the gap loop, never by silent re-layout.

## 9) Notes

**[Provenance note 2026-06-12 — D-051; no new edge]:** the value I = 1 displayed at the
M_exist top-left entry (assembled here from DEF_003_004's column) is DERIVED at
THM_002_007 (the binary-cut First Existence Count); the derivation is inherited
transitively through DEF_003_004's re-point (D-051) — no dependency edge to THM_002_007 is
added (this card displays the value, it does not consume it as load-bearing content). The
existence ground remains AX_001_001 (now: existence is relational distinguishability).

Forward pointers only (TD-020): S03 adds the geometry column by IMPORT cards into NEW
positions (M_cell 3×3 — the D-028 scope-guard card lands there, S03's obligation); S04
adds the runtime column (M_rank 3×4; the rank-axis forcing THM_003_016 proves the axis
must exist, content delivered at S04). The bottom-row residual rule is the Q6 theorem
target — drafting awaits the librarian corpus search; this card's bottom-row reading is
ruled content with derivation status OPEN. Rename discipline for any SPINE_3 3×4
"M_cell" lineage: import-renamed per D-030.Q3, mechanized at CONV_S02_013's witness.
