---
id: THM_005_017
publication_dependencies: ["DEF_004_012","DEF_005_005","DEF_005_007","THM_004_016","THM_005_016"]
---

# Exact x = 3/4 family + the {3,4} staircase — N_tick(3) record-absent, N_tick(4) = 1; ε₄ = 4Ω − 1 = 3 − 4π/(3√2) consumed from registry; tick gaps ∈ {3,4} ⟺ 2√2 < π < 9√2/4 (symbolic, P8-clean); the four-cell cluster-clock {26,27} FENCED (different reading)

## 0) Governance & Scope Lock

Theorem card with complete exact proofs (§4). This is ACCOUNT COMPLETION, NOT real-number floor
math (D-049/TD-028): N_tick(x) = ⌊x·Ω⌋ is the COUNT OF COMPLETED UNIT-ACCOUNTS by cycle x (units
of I = 1 — DEF_005_005), and the {3,4} gaps are the intervals between successive unit-account
COMPLETIONS. The exact arithmetic survives unchanged; the grounding is account completion. Exact
symbolic arithmetic only (2√2, 9√2/4, ε₄ = 3 − 4π/(3√2) symbolic; P8). N_tick(3) is the
record-absent predicate (no unit-account completed by cycle 3), never 0. The single-cell
{3,4} staircase is THIS card's; the four-cell cluster-clock {26,27} is FENCED (GUARD_005_001,
consumed only through THM_004_016 forward — named here only to keep the two distinct). The /4 in G
is untouched. Layer ceiling S04.

## 1) Plain language

At the exact void share Ω = 1 − π/(3√2), the unit-accounts complete in a rigid rhythm: no whole
unit completed by cycle 3, the first completion at cycle 4, and from then on the gaps between
successive completions are always 3 or 4 cycles — never 2, never 5. These gaps are the intervals
between successive unit-account completions, not the floor of an arbitrary real (D-049); this
two-letter alphabet {3,4} holds for exactly the π-window 2√2 < π < 9√2/4, which the true value of
π sits inside (a risky, checkable prediction at the NOT-fitted Ω). A related but DIFFERENT clock —
the slow four-cell pooled one — runs on completion gaps {26,27}; that is a separate reading with
its own window, kept strictly apart from this single-cell one. The overflow rate ε₄ = 4Ω − 1
is the registry quantity that makes 4Ω = 1 + ε₄; this page consumes it, never restates it.

## 2) Statement

(Ω = 1 − π/(3√2), DEF_004_012, strictly positive; ε₄ = 4Ω − 1 = 3 − 4π/(3√2), consumed from the
registry; N_tick(x) = ⌊x·Ω⌋ = the COUNT OF COMPLETED UNIT-ACCOUNTS by cycle x, DEF_005_007 /
DEF_005_005 — account completion, not the floor of a real, D-049/TD-028.)

**(a) The exact x = 3/4 family.** N_tick(3): **no tick posted by cycle 3** (record-absent — the §0
re-typing; no unit-account has completed by cycle 3, since 3Ω < 1); N_tick(4) = 1 (⌊4Ω⌋ = 1 — one
completed unit-account by cycle 4). And **4Ω = 1 + ε₄** with ε₄ = 4Ω − 1 = 3 − 4π/(3√2) (consumed
from the registry, never restated — F-035/F-020 closure).

**(b) The {3,4} staircase.** The tick gaps — the intervals between successive unit-account
completions — lie in {3,4}:

$$\text{gaps} \in \{3,4\} \;\Longleftrightarrow\; \tfrac14 < \Omega < \tfrac13 \;\Longleftrightarrow\; 2\sqrt2 < \pi < \tfrac{9\sqrt2}{4},$$

symbolic and P8-clean (F-130's bound correction carried: the 9/(4√2) misprint DID fail this exactly;
the correct upper bound is 9√2/4).

**(c) The four-cell cluster-clock {26,27} is the SLOW-HAND sibling and is FENCED (GUARD_005_001).**
The slow-hand ε₄ gap word ⊆ {26,27} ⟺ 231√2/104 < π < 20√2/9 is a DIFFERENT reading (the four-cell
pooled clock, F-160/F-161), consumed only through THM_004_016 forward, identified with the
single-cell {3,4} NOWHERE — a SEPARATE object with its own falsifier (decided strictly twice). It is
NOT this card's claim; named only to keep the two distinct.

## 3) Input contract (GR-1)

Objects: Ω = 1 − π/(3√2) ← DEF_004_012 (exact, strictly positive); ε₄ = 4Ω − 1 ← registry (the
cluster overflow rate, consumed); N_tick(x) = ⌊x·Ω⌋ ← DEF_005_007 (via the equivalence THM_005_016);
the four-cell cluster reading ← THM_004_016 (REFERENCE only, the forward-only fence). Domains: exact
symbolic forms over the runtime residual scale; symbolic π, √2. Quantifiers: universal over tick
gaps at Ω = Ω_pack. Finite-N surface: the §7 gap-word sweep (exact). Limit mode: none.

## 4) Proof (complete, exact)

**(a)** From Ω = 1 − π/(3√2), multiply by 3: 3Ω = 3 − π/√2 (exact, since 3·π/(3√2) = π/√2). From
the §4(b) bracket 2√2 < π < 9√2/4, divide by √2: π/√2 ∈ (2, 9/4), so 3Ω = 3 − π/√2 ∈ (3/4, 1) —
strictly below 1: **no tick posted by cycle 3** (record-absent, the §0 re-typing; ⌊3Ω⌋ has no posted
tick). Multiply Ω by 4: 4Ω = 4 − 4π/(3√2) = 1 + (3 − 4π/(3√2)) = 1 + ε₄ (exact, by ε₄'s registry
definition ε₄ = 4Ω − 1); and ε₄ ∈ (0,1) (from the §4(b) bracket: 2√2 < π < 9√2/4 ⟹ 0 < ε₄ < 1), so
⌊4Ω⌋ = 1: **N_tick(4) = 1**. ∎(a)

**(b)** Tick gaps lie in {3,4} iff the irrational rotation by Ω (the Beatty/three-distance structure
of ⌊x·Ω⌋) has step alphabet {3,4}, which holds iff 1/4 < Ω < 1/3 (the gap is ⌈1/Ω⌉ or ⌊1/Ω⌋, and
1/Ω ∈ (3,4) iff 1/4 < Ω < 1/3). Substituting Ω = 1 − π/(3√2):
1/4 < 1 − π/(3√2) ⟺ π/(3√2) < 3/4 ⟺ π < 9√2/4 (multiply by 3√2, exact); and
1 − π/(3√2) < 1/3 ⟺ 2/3 < π/(3√2) ⟺ 2√2 < π (multiply by 3√2, exact). So gaps ∈ {3,4} ⟺
2√2 < π < 9√2/4, exact and symbolic. (F-130: the 9/(4√2) form is NOT 9√2/4 — it fails the exact
check; the corrected bound is carried.) ∎(b)

**(c)** The four-cell cluster-clock {26,27} reading uses the four-cell pooled structure (F-160/F-161),
a DIFFERENT object consumed through THM_004_016 forward-only (GUARD_005_001). Its bracket
231√2/104 < π < 20√2/9 is a separate exact inequality. This card neither proves nor consumes it as
the {3,4} reading; the two are fenced. ∎(c, typing — the {26,27} claim is GUARD_005_001-fenced, not
this card's)

**What this proof does NOT use:** no backward export of the four-cell '4' (D-025.R10); no
consumption of the four-cell condition as ground for the single-cell {3,4} (THM_004_016 is the
forward-only fence reference); no decimal; no q_share.

## 5) Scope & non-overclaim

Proves the exact x = 3/4 family, the {3,4} staircase ⟺ 2√2 < π < 9√2/4, and 4Ω = 1 + ε₄. Does NOT
claim: the four-cell cluster-clock {26,27} (FENCED — a separate object, GUARD_005_001); any backward
export of the /4 (D-037's reopen runs through S06); the limit (THM_005_019). The {3,4} and {26,27}
brackets are kept as DISTINCT exact interval inequalities (D-048 Q-G; the F-161 standard). Guard
references: GUARD_001_008 (via GUARD_005_008), GUARD_005_008, GUARD_005_001.

## 6) Per-symbol accounting

Local: x (cycle count — card-local); the gap-word brackets (card-local exact intervals). Imported:
ε₄ ← registry (the cluster overflow rate, consumed not restated); Ω ← DEF_004_012; N_tick ←
DEF_005_007 (via THM_005_016); the unit account I = 1 and the completed-unit-count account typing
(N_tick = count of completed unit-accounts; gaps = intervals between completions) ← DEF_005_005
(the D-049 account-completion grounding); the four-cell cluster reading ← THM_004_016 (reference,
forward-only fence); π, √2 ← registry (symbolic). Undeclared: none.

## 7) Verification (witness SPEC — for the witness author; Witness-Author docstring line
per witness_harness/README rule 6; resolve the card eq_spine-first, rule 7; P12: this
card's author writes no script)

Witness (exact-symbolic, symbolic π/√2 and exact rationals; no floats): (1) the x = 3/4 family:
assert 3Ω ∈ (3/4, 1) (no tick by 3 — record-absent, not ⌊3Ω⌋ = 0 as a held value) and ⌊4Ω⌋ = 1
exactly; assert 4Ω = 1 + ε₄ with ε₄ = 3 − 4π/(3√2) (canonical-form equality, the registry value
consumed — recomputed, not inherited); (2) the {3,4} bracket: assert gaps ∈ {3,4} ⟺ 1/4 < Ω < 1/3 ⟺
2√2 < π < 9√2/4 by exact symbolic inequality certification (no float comparisons; the F-130 burn:
assert the 9/(4√2) form FAILS the exact check and 9√2/4 PASSES — both computed, the misprint-class
caught); (3) a finite gap-word sweep at Ω = Ω_pack: compute K(n) − K(n−1) for n up to a declared
bound and assert every gap ∈ {3,4} (the section P0 falsifier 2 made executable); (4) DISTINCTNESS
(GUARD_005_001): assert the {26,27} bracket 231√2/104 < π < 20√2/9 is computed as a SEPARATE
interval and is NOT identified with the {3,4} bracket (value-level: the two intervals are distinct
objects — the fence made executable); (5) representation audit: symbolic π/√2, no decimals; (6)
account-completion reading (D-049/TD-028): assert N_tick(x) = ⌊x·Ω⌋ reads as the COUNT OF COMPLETED
UNIT-ACCOUNTS and the {3,4} gaps as intervals between successive completions — under a change of
measuring unit by a positive factor c, assert the completed-unit count and the gap alphabet verdict
are INVARIANT value-level (DEF_005_005's invariance; the decimal display moves, the account reading
does not). Can-fail
(--corrupt): in memory, set the upper bound to 9/(4√2) (the misprint); assertion (2) MUST fail
(the {3,4} bracket fails the exact check at the wrong bound).

## 8) Falsifiers

As in frontmatter — the {3,4} alphabet falsifier (the section P0 item 2; one-loop, can lose), the
ε₄ exact-value falsifier, and the SEPARATE cluster-clock {26,27} falsifier (a distinct object,
F-161, decided strictly twice — GUARD_005_001 fences it). Loss handling per D-025.R26: report, block
downstream (#13/#14 and S08's banding inheritance consume this), RSM classifies.

## 9) Notes

**Account completion, not real-number floor math (D-049/TD-028).** This card's RE-GROUNDING: the
{3,4} staircase is unchanged in its exact arithmetic — the brackets, the π-window
2√2 < π < 9√2/4, ε₄ = 4Ω − 1 = 3 − 4π/(3√2), and the {26,27} cluster-clock bracket (FENCED) are
identical to the floor-math statement — but N_tick(x) = ⌊x·Ω⌋ now reads as the COUNT OF COMPLETED
UNIT-ACCOUNTS by cycle x (units of I = 1, P1 — DEF_005_005), never 'the floor of a real number',
and the tick gaps are the intervals between successive unit-account COMPLETIONS. The TD-028 flag
marks the re-typing target's resolution: the printed decimal is scale-dependent display, the
threshold the invariant. The four-cell {26,27} cluster-clock stays a DISTINCT, separately-fenced
object (GUARD_005_001's four-cell fence, consumed only through THM_004_016 forward) — the
re-grounding touches the GROUNDING language, never the exact brackets or the fence.

Forward pointers only (TD-020): THM_005_019 (the limit lim N_tick(x)/x = Ω); MODULE/#14 (the frozen
100-tick table exhibits the {3,4} gaps); S08 inherits the banding at exactly Ω_pack (the
risky-prediction, dossier seam F-4); S06 reads the four-cell cluster-clock {26,27} junction signature
(seam F-4 — the DISTINCT object, GUARD_005_001). The ε₄ value is consumed from the registry (F-035/
F-020 closure), never restated. Drafted, pending witness author → verifier → drift → RSM gate.
