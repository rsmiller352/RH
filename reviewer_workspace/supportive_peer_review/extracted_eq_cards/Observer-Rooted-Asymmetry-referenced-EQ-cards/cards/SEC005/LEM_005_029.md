---
id: LEM_005_029
publication_dependencies: ["LEM_005_027","LEM_005_028","THM_005_026"]
---

# Native never-exact strengthening (TRANSITIONAL) — under the current π-volume residual Ω = 1 − π/(3√2), the native trajectory never settles exactly (LEM_005_027) and its history is recoverable positionally (LEM_005_028); a strengthening of THM_005_026's recurrence, NOT its ground; transitional (rides the π²-irrationality import)

## 0) Governance & Scope Lock

**LEM card (S04 recurrence split) — the TRANSITIONAL native strengthening.** The core recurrence theorem (`THM_005_026`) does NOT need Ω to be irrational. This card records the stronger native result that follows IF the currently promoted geometric residual `Ω = 1 − π/(3√2)` is used together with the transitional classical import behind `LEM_005_027`. It is intentionally **not** the ground of recurrence. TRANSITIONAL: it rides the π²-irrationality import (`LEM_005_027 → IMPORT_004_024`).

Banned: claiming recurrence / tick-forcing (that is `THM_005_026`, import-free); presenting this as final no-import ground; physical time/clock/rate; a stored zero.

## 1) Plain language

The recurrence theorem does not need Ω irrational. But under the current π-volume residual, Ω IS irrational, so the native trajectory never lands exactly on a whole unit: every native escapement leaves a strictly-positive remainder, and the residual position determines the tick count (history recoverable, no stored archive). This is a sharpening of runtime behaviour, not the reason runtime recurs — and it is transitional, because it rides the classical π²-irrationality import.

## 2) Statement

On the native `Ω = 1 − π/(3√2)` trajectory from the empty-history start:

1. **(Never-exact.)** No positive integer `K` makes `K·Ω` a whole unit: `K·Ω ∉ ℤ` for all `K ≥ 1` (`LEM_005_027`; else `π ∈ ℚ·√2 ⟹ π² ∈ ℚ`, contra `IMPORT_004_024`). Hence the native discharge never settles with absent residual — every native escapement is strict, leaving a strictly-positive remainder.
2. **(Recoverability.)** The residual map `K ↦ frac(K·Ω)` is injective (`LEM_005_028`); so the current residual position determines the cycle/tick count — history is recoverable positionally, with no stored archive.
3. These are **strengthenings** of the runtime account. They do NOT force recurrence — recurrence is already supplied, import-free, by `THM_005_026`.

## 3) Input contract (GR-1)

**Consumed (3 edges):** the import-free recurrence base (NOT re-proved) ← `THM_005_026`; Ω irrational, `K·Ω ∉ ℤ` (the transitional π²-import) ← `LEM_005_027`; the injective residual map `K ↦ frac(K·Ω)` (recoverability) ← `LEM_005_028`. **Named (NOT consumed):** `IMPORT_004_024` (the π²-irrationality trust point behind LEM_005_027 — transitional). **No recurrence claim; no physical time; no stored zero; no final-no-import claim.**

## 4) Proof

**(1)** By `LEM_005_027`, `Ω = 1 − π/(3√2)` is irrational, and no positive-integer multiple `K·Ω` is an integer. Therefore the native empty-history residual trajectory never lands exactly on the unit boundary with no remainder — every native escapement is strict.

**(2)** By `LEM_005_028`, `Ω` irrational makes `K ↦ frac(K·Ω)` injective on the native trajectory. Therefore the current residual position determines the cycle/tick count: history is recoverable from position, not stored as an archive.

**(3)** None of this forces recurrence — recurrence is supplied import-free by `THM_005_026` (from the S01 no-terminal/no-last engine + tick-uniqueness). This card only sharpens what native recurrence looks like under the current π-volume residual. ∎

## 5) Transitional status

This card depends on the transitional import path through `LEM_005_027` (`IMPORT_004_024`, π² irrational). If the √2-side re-grounding program replaces the classical π²-irrationality import with an internal/lattice-side residual, this card should be amended or superseded by the internal replacement. **If a future re-grounded residual share is rational, this card's never-exact and recoverability claims FAIL** (a real falsifier, not a prose issue), **but `THM_005_026`'s recurrence remains intact.**

## 6) Scope & non-overclaim

This card does not prove tick recurrence (that is `THM_005_026`). It does not build physical time, a physical clock, or a rate in seconds. It does not make Ω a mass, energy, density, or an observer-scale quantity. It is transitional and NOT final forced ground.
