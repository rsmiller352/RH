---
id: DEF_005_004
publication_dependencies: ["DEF_002_004","DEF_005_003","GUARD_001_008"]
---

# The unit posting — a strictly-positive unit POSTING per discharge; the VALUE FOLLOWS from account completion (POINTS at THM_005_009, never declared — D-049); the {0,1} indicator RETIRED

## 0) Governance & Scope Lock

Definition card: TYPES the unit posting strictly positive and POINTS at THM_005_009 for its
value, and nothing else — no discharge dynamics (THM_005_010), no update rule (#7), no count
readout (#10–#13). Exact forms only. The posting is a strictly-positive unit; its VALUE ΔI = 1
FOLLOWS from account completion (THM_005_009 — FORCED by the Law of Existence, never declared,
D-049). "No tick this cycle" is the absence-of-posting predicate, never ΔI = 0; the {0,1}
indicator is retired. Layer ceiling S04; this card SETS UP S05 (forward pointer only, SQ-2).

## 1) Plain language

When the running cycle's leftover crosses a whole unit, one tick fires — and a tick is one
strictly-positive unit posted, never "a one" sitting next to "a zero." There is no
zero-or-one switch: either a posting happens (a strictly-positive unit, value one) or no
posting happens (and then no event exists — there is nothing to count, not a zero counted).
This is the same counter the next section will call the existence counter: a count that
starts at one and only ever goes up by strictly-positive units. Why exactly one unit? Not
because anyone declared it and not because of an old symmetry argument (that argument quietly
assumed its own answer — F-182). It is because, when a leftover finishes one whole unit of
existence, that completed unit must be recorded (the Law of Existence), and exactly one whole
unit completed — so exactly one unit-token posts, and the "one" simply falls out. The forcing
is proved on the next page (THM_005_009); this page just types the posting as one
strictly-positive unit and points there for the value. A signed "−1" version from the old work
is not a quantity at all; its real content is a transfer, handled on the discharge page.

## 2) Statement

**The unit posting (typed strictly positive; the value FOLLOWS from THM_005_009).** A
tick/discharge posts a **strictly-positive unit**. Its value is the single FORCED completed
unit-token:

$$\Delta I = 1 \qquad(\text{FORCED by account completion — THM\_S04\_019; not declared}).$$

The posting is strictly positive by type; no ΔI = 0 object exists. **"No tick this cycle"** is
the **absence-of-posting predicate** — no event exists (GUARD_001_008 pred. 2; GUARD_005_008) —
NOT ΔI = 0. The `{0,1}` indicator of the old work is a D-026 reversion symptom and is
**retired**: there is no switch; there is a posting or its absence (a typed predicate).

**The counter the posting feeds.** The S04 escapement/tick counter is THE SAME OBJECT as the
S05 existence counter — strictly positive, counted in completed units of I = 1 (P1). The
archive floor form's "I = 0" re-types as no-event/no-posting (record absent). **S04 sets up
S05** (the S05 existence-counter card (SQ-2) consumes this posting — SQ-2; forward pointer
only).

**Value source (the FORCING, not a declaration — D-049).** The value 1 FOLLOWS from account
completion (THM_005_009): the residual is a unitized ratio R_acc against the unit account
I = 1 (DEF_005_005); when it reaches the unit, one unit of existence completes; the Law of
Existence forces the completed unit to post; exactly one completed unit-token posts, so ΔI = 1
FOLLOWS. The integrality is DERIVED (existence is counted in completed units, P1), DISSOLVING
F-182's "integrality is a fiat" finding. This card carries NO declared value — it TYPES the
posting and POINTS at THM_005_009. (The D-048 Q-C "declared via the ℤ≥1 codomain" stopgap is
SUPERSEDED by D-049; note4's symmetry argument STAYS circular, F-182 — not used.)

## 3) Input contract (GR-1)

Objects: ΔI (the posting — typed strictly positive, before any arithmetic, TD-009); its value
← THM_005_009 (the account-completion forcing — this card POINTS there for the value); the
discharge event that books it ← THM_005_010 (forward — this card supplies the magnitude type,
THM_005_010 supplies the pairing, THM_005_009 supplies the value); the register the posting
transfers from ← DEF_005_003; I (the existence counter the posting feeds — the same object,
counted in completed units of I = 1). Domains: the count book (completed units of I = 1).
Quantifiers: none (a typing). Finite-N surface: the §7 well-formedness checks. Limit mode: none.

## 4) Well-formedness (DEF obligation)

- **Posting strictly positive by type.** The posting magnitude is a strictly-positive unit;
  ΔI = 0 is not an admissible value, and "no posting" is the absence-of-posting predicate, not
  a zero value (GUARD_005_008). The type is well-formed because the count is taken in completed
  units of I = 1 (P1; DEF_005_005) — a strictly-positive unit structure, 1 its least element,
  no zero object.
- **Value FORCED, not declared (D-049; THM_005_009).** The value 1 is NOT declared on this
  card; it FOLLOWS from account completion (THM_005_009): one completed unit of existence must
  post, exactly one completed unit-token, so ΔI = 1. The integrality is DERIVED (existence is
  counted in completed units, P1), dissolving F-182's "integrality is a fiat" finding. This is
  the honest type-selection: a DEF that TYPES the posting and POINTS at the forcing theorem,
  never a DEF declaring the value via a codomain (the D-048 Q-C stopgap, SUPERSEDED by D-049 —
  no declarations) and never a THM duplicating THM_005_009's forcing.
- **The signed sibling re-typed, not consumed.** PC-011's "R_min = −1" is the signed
  sibling: as notated it CONTRADICTS(P3) — there is no negative quantity. Its content is the
  oriented transfer (one unit OUT of the residual register, one unit INTO the event count —
  the −1/+1 oriented pair), which is THM_005_010's double-entry, entered through CONV_S01_009
  (orientation × magnitude) and DEF_002_004 (the transfer). This card declares only the
  posting MAGNITUDE (a strictly-positive unit); the pairing is THM_005_010's.

## 5) Scope & non-overclaim

Types the posting magnitude as a strictly-positive unit and POINTS at THM_005_009 for its
value; decides NOTHING about WHEN a tick fires (the threshold-crossing trigger is THM_005_010),
the update rule (#7), or any count readout (#10–#13). Does NOT claim the value independently
(it FOLLOWS from THM_005_009's account-completion forcing — D-049) and does NOT declare it (the
D-048 Q-C codomain-declaration is SUPERSEDED). Does NOT claim: that note4's symmetry argument
forces it (F-182 STANDS — circular); any S05 content (the existence counter is the same object
but its Law of Existence card is S05's, SQ-2); any signed quantity. Guard references:
GUARD_001_008 (via GUARD_005_008), GUARD_005_008. Candidate-interpretation honesty: the "same
object as the S05 counter" reading is drafted under D-049's account-completion ground, the S05
card pending at its own layer.

## 6) Per-symbol accounting

Local: none beyond the registered ΔI (the posting). Imported: ΔI — registered 2026-06-12
(frontmatter R17 audit); the value ΔI = 1 (the forced single completed unit-token) ←
THM_005_009 (the account-completion forcing); the transfer ground (orientation × magnitude, the
oriented pair) ← DEF_002_004 / CONV_S01_009 (named for the signed-sibling re-typing; the pairing
is THM_005_010's); the strict-positivity typing and the absence predicate ← GUARD_001_008 (via
GUARD_005_008); the register ← DEF_005_003; I (the existence counter the posting feeds) ← the
unit; I's native role is invariant (M_base row 1, THM_003_018 — the retired CONV_S02_014 N1 class is superseded), the same object, counted in completed units of
I = 1. Undeclared: none.

## 7) Verification (witness SPEC — for the witness author; Witness-Author docstring line
per witness_harness/README rule 6; resolve the card eq_spine-first, rule 7; P12: this
card's author writes no script)

Witness (exact-symbolic, exact integers — Fraction/int, no floats): (1) typing: assert the
posting magnitude is a strictly-positive unit (value-level: ΔI = 1, the single completed
unit-token; an attempt to construct ΔI = 0 must be rejected as not-an-admissible-value, not
stored) — note the value 1 is the one completed unit (THM_005_009's forced output, consumed
here as the magnitude type), not a declared constant; (2) absence-vs-zero: model a sequence of cycles
some of which post and some of which do not; assert that a no-posting cycle is represented
as the ABSENCE of a posting event (no event object exists for that cycle) rather than a
posting of magnitude 0 — the support-membership / event-existence predicate, value-level,
the §0/D-043 honesty-structure check (the retired {0,1} indicator must be ABSENT from the
representation — assert there is no zero-valued posting object anywhere); (3) representation
audit: every posting magnitude strictly positive, no decimals. Can-fail (--corrupt): in
memory, represent a no-posting cycle as a posting of magnitude 0 (re-introducing the {0,1}
indicator); check (2)'s no-zero-posting assertion MUST fail. The pairing of the posting with
the residual-register outflow is NOT witnessed here (THM_005_010's witness); this card
witnesses the posting's strictly-positive type only.

## 8) Falsifiers

As in frontmatter — the strict-positivity falsifier routes through GUARD_005_008 to the
§0/D-043 foundation falsifier; the symmetry-balance falsifier (born with this card, D-046)
fires on an unpaired posting and routes through the D-042 law-violation protocol (held true,
diagnosed), never demoting a law. Loss handling per D-025.R26: report, block downstream
(#6–#13 consume the posting), RSM classifies.

## 9) Notes

**The re-grounding (D-049 — SUPERSEDES D-048 Q-C).** The "declared form" resolution is
RETRACTED ("I dont intend to have declarations" — D-049). The value ΔI = 1 is FORCED by account
completion (THM_005_009 — the Law of Existence forces a completed unit to post; exactly one
completed unit-token, so ΔI = 1 FOLLOWS). This card RE-GROUNDS: it TYPES the strictly-positive
posting and POINTS at THM_005_009 for the value, never declaring it. F-182 STANDS (note4's
symmetry argument is still circular); THM_005_009 is the SEPARATE, non-circular account-completion
route, which DISSOLVES F-182's "integrality is a fiat" finding (integrality follows from existence
being counted in completed units, P1). The non-unit/inconsistent case is a MISSING operation
(D-042 amendment 4), never a demotion of I_base = 1.

Forward pointers only (TD-020): THM_005_010 (the discharge that posts ΔI as one end of a
double-entry transfer); DEF_005_011 (the update rule); the S05 existence-counter card (SQ-2)
(the same counter, D-043.Q-d — SQ-2). The L05-family sweep (GAP-S04-N, OI-035(a)) is OWED
before PC-011 (and the PC-008/009/010 stack at THM_005_010/§0) GROUNDS anything; this card
draws on the FORMAL spine deps (DEF_002_004, GUARD_001_008, DEF_005_003) and carries PC-011
as lineage/motivation only — drafted, pending witness author → verifier → drift → RSM gate.
