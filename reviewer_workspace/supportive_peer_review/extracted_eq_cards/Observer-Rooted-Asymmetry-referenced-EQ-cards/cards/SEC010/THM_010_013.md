---
id: THM_010_013
publication_dependencies: ["LEM_010_011","THM_010_010"]
---

# The readout count S_flow = (c−1)/c = 2/3 (FORCED) — the C4-top readout invariant, as c is the C2-top count and η is the C3-top edge: S_flow is the observer's content-distinguishable count of the twin-merged four-cell readout (the invariant flow of time read out); the readout column's other entries Ψ, Λ CARRY it (forward, deferred — the one honest difference from η, the carriers un-built). NO power laws, ALL COUNTING; (c−1)/c = 2/3 FORCED. DERIVED, not assigned; the continuous Koide Q = 2/3 is the gated aperture rendering

## 0) Governance & Scope Lock

**THM card (FORCED value-theorem) — the readout count S_flow = (c−1)/c = 2/3; the C4-top readout invariant, worded as `c` is the C2-top count (`DEF_003_015`) and `η` is the C3-top edge (`THM_004_019`).** Forces `S_flow = (c−1)/c = 2/3` (the observer's content-distinguishable count of the twin-merged four-cell readout — `LEM_010_011`, the promoted count). Just as the C2 column reads *"c is a count; E, G carry it, not independent constants"* (`DEF_003_015`) and the C3 column reads *"η is the edge; D, Ω carry it"* (`THM_004_019`), the C4 column reads: **S_flow is the readout invariant; Ψ, Λ carry it** — but with the **one honest difference**: Ψ, Λ are **un-built** (the 4th-column gravity), so the carrying relation is **FORWARD**.

- **S_flow := the observer's content-distinguishable count** of the twin-merged four-cell readout = **(c−1)/c**; at the forced `c = 3` (`THM_003_012`, via `LEM_010_011`), **S_flow = 2/3, FORCED**. **Derived (by counting the readout), never assigned** — as `c` is the count from `THM_003_012` and `η` the edge from `THM_004_019`.
- **What it IS (the identity):** S_flow is the **invariant flow of time** (`THM_010_010` §9(viii) — the count's monotone, `ι_or`-invariant directedness, *"an objective invariant, not the observer-relative σ-reference"*) **read out** as the observer's content-distinguishable count. The identity (an invariant, not σ) + the value (the count 2/3) are the same object: the flow, and the count it reads as.
- **Ψ, Λ carry S_flow — FORWARD (the one honest difference from η).** η's placement could say *"D, Ω carry η"* because D, Ω were promoted; here `Ψ` (= the mass-readout, `|Ψ| = m·Ω` **deferred** OI-052) and `Λ` (= the readout-axis closure / `G_readout`, the **4th-column gravity**, OI-063 step 3) are **un-built**, so *"Ψ, Λ carry S_flow"* is a **named forward pointer**, completed when the carriers exist. The C4-**top** is placed now, fully forced.
- **NO power laws — all counting.** `(c−1)/c` is a **count ratio** (the `k−1` count precedent), not a power/quadratic — exactly as the C2 `c²` is *"two applications, not a squaring"* (`DEF_003_014`/TD-019) and the C3 edge is the lattice's counted minimal norm (`THM_004_019`). No `S_flow`-power law.
- **The rendering fence (mirrors η).** `S_flow = 2/3` is the **DISCRETE** readout count (the substrate). The continuous Koide `Q = 2/3` (`b/a = √2`, `a + b·cos`) is the **APERTURE RENDERING** (forward, gated RO-G.2 / `GATE_008_013`), never the basis — exactly as `η = √2` is the discrete edge and Koide `b/a = √2` its rendering (`THM_004_019`). The seed `S_rt = τ·Ω` is **retired** (RSM 2026-06-22; no card grounds it as the flow).

Banned: S_flow **assigned**; any **power law**; **τ·Ω** re-introduced; a **wave/cosine/power** substrate (S_flow is the DISCRETE count; Koide is the gated rendering — R1/R3/TD-003); **σ-typing of the arrow** (the arrow is `ι_or`-invariant — TD-041); **physical mass/energy/seconds** (TD-019/TD-042); **Ψ/Λ value** or a **built carrying relation** (forward only); **editing any promoted card** (amendment-lock); a **zero/negative** (2/3 ∈ (0,1); the twins are units I=1 — TD-040). **[PROMOTED 2026-06-22 — gauntlet cleared (spine-architect → independent witness → verifier F-280 → drift → RSM gate); status: formal.]**

## 1) Plain language

The readout column is where the *observer* enters the matrix, and the new thing it brings is one number: **what fraction of the cells an observer can tell apart.** The four cells (`THM_005_023`) are not four distinct standpoints — the youngest two are **twins**, born in the very same closure-event at tick 8, of equal existence-depth, so they read the **same** (`LEM_010_008`); only their orientation `σ` distinguishes them. So of the `c = 3` cells an observer reads, **one** is the co-present twin it cannot tell apart by content, and **the other two** (the older past, `A > B`, `LEM_010_005`) it can. Two distinguishable out of three: **(c−1)/c = 2/3.** Call it **S_flow**.

This is worded exactly as the second and third columns are. There, `c` is *a count* and `η` is *an edge*, and the rest of each column just **carries** them (`DEF_003_015`/`THM_004_019`: "not independent constants"). Here the same: **S_flow is the readout count**, and the column's other entries (`Ψ`, the mass-readout; `Λ`, the readout's gravity) will **carry** it. There is one honest difference from the η column: there, the carriers (`D`, `Ω`) were already built, so we could write "D, Ω carry η" on the spot. Here `Ψ` and `Λ` are **not built yet** (they come with the fourth-column gravity), so "Ψ, Λ carry S_flow" is a **promise pointing forward**, not a relation we write today. We place the **top** — the count — now; it is fully forced.

And what *is* this count, underneath? It is the **flow of time, read out.** The flow of time is the plain fact that the count of completed records only ever **increases** — the same direction for everyone, fixed by non-erasure, not by anyone's point of view (`THM_010_010`). The observer reading the four cells reads exactly that flow as the fraction it can resolve: `2/3`. (What it is *not*: the orientation `σ` — that is the genuinely observer-relative thing, the *separate* axis, and it is **not** the arrow. We never confuse the two.)

What we do **not** do: write a *power law*, or let the smooth "Koide wave" be the ground. `(c−1)/c` is a **count** — two of three — not a square of anything. The continuous Koide reading `Q = 2/3` is the **rendered picture** of this same count (a later, gated comparison), never the substrate — exactly as the smooth `b/a = √2` is the rendered picture of the discrete edge `η`. And the old seed value `τ·Ω` is gone: nothing grounds it as the flow.

## 2) Statement

> **(THM_010_013 — S_flow = (c−1)/c = 2/3 forced; the C4-top readout invariant, as c is the C2-top count and η the C3-top edge.)** In the realized four-cell readout (`THM_005_023`; within the one existence, `AX_001_001` I = 1; the observer the relational reference, `ROLEDEF_008_014`):
>
> - **(1 — the readout count, FORCED.)** Define **S_flow := the observer's content-distinguishable count of the twin-merged four-cell readout**. Of the `c = 3` read cells (`THM_003_012`), exactly **one** is the co-present, content-indistinguishable twin (`THM_005_023` born-together; `LEM_010_008` the tie — distinguishable only by `σ`), and the other **c − 1 = 2** are the content-distinguishable past (`A > B`, `LEM_010_005`). Hence (by `LEM_010_011`, the promoted count):
> $$\boxed{\;S_{\text{flow}} = \frac{c-1}{c} = \frac{2}{3}\;}\quad\text{— FORCED (no other value admissible), not assigned; observer-independent.}$$
> - **(2 — what it IS: the invariant flow of time, read out.)** S_flow is the **invariant flow of time** (`THM_010_010`: the count's monotone, `ι_or`-invariant directedness — fixed by non-erasure + ΔI=+1, the same for every observer; the §9(viii) C4-top candidate) **read out** as the content-distinguishable count. It is the **objective** invariant, **NOT** the observer-relative `σ`-reference (which is the separate, preserved axis — the co-present twin is `σ`-distinguishable, never erased). The C4-top readout invariant the way `I` is the C1-top unit, `c` the C2-top count, `η` the C3-top edge (`DEF_003_015`/`THM_004_019` idiom).
> - **(3 — Ψ, Λ carry it; FORWARD — the one honest difference from η.)** The readout column's `Ψ` (mass-readout, `|Ψ| = m·Ω`) and `Λ` (readout closure, `G_readout`) **carry** S_flow as `E`, `G` carry `c` and `D`, `Ω` carry `η` ("not independent constants"). But `Ψ`, `Λ` are **un-built** (deferred — OI-052; the 4th-column gravity, OI-063 step 3), so the carrying relation is a **named forward pointer**, not a built relation. The **top** is placed now (the value forced); the carrying relation follows when the carriers exist.
>
> **No power law — all counting** (`(c−1)/c` a count ratio, not a power). The continuous Koide `Q = 2/3` (`b/a = √2`) is the gated **aperture rendering** (forward, RO-G.2 / `GATE_008_013`), never the substrate (R1/R3/TD-003). The seed `S_rt = τ·Ω` is **retired**. `2/3` exact rational (P8).

## 3) Input contract (GR-1)

**Consumed (2 edges):** the content-distinguishable count `(c−1)/c = 2/3` (the forced value; `c = 3` and the twin-merge transitive within it) ← `LEM_010_011`; the invariant flow of time (the C4-top identity — the count's `ι_or`-invariant directedness, the §9(viii) candidate) ← `THM_010_010`. **Named (NOT a dependency):** `THM_003_012` (c = 3 forced — transitive via `LEM_010_011`); `THM_005_023` (twins born together — transitive); `LEM_010_008` (the tie — transitive); `LEM_010_005` (depth order — transitive); `ROLEDEF_008_014` (the observer role — transitive); `THM_010_006` (content-not-σ — transitive); `LEM_010_012` (the spectrum sibling, same twin-merge); `GATE_008_013` (RO-G.2 — the rendering fence); `DEF_003_015` (the C2 count idiom); `THM_004_019` §9(vi) (the C3 edge idiom + the cross-link naming `S_flow = (c−1)/c = 2/3`); `DEF_010_014` (the M_rank 3×4 ladder — placement is `DEF_010_014`); `THM_003_016` (the 4th axis forced); the retired seed `S_rt = τ·Ω`; `Ψ`, `Λ` (the FORWARD carriers, deferred). **Domains:** dimensionless count fraction, exact ℚ. **No assigned S_flow; no power law; no τ·Ω; no wave/cosine/power substrate; no σ-typed arrow; no physical mass/energy/seconds; no Ψ/Λ value / built carrying relation; no promoted-card edit; no zero/negative.**

## 4) Proof (complete — S_flow = (c−1)/c = 2/3 forced; the identity; the forward carrying-relation)

**Step 1 — c = 3, FORCED.** By `THM_003_012`, `c = 3` is the minimal register-position count of a non-vacuous persisting closure (a genuine consequence of `THM_002_011`, not a coincidence). ∎(1)

**Step 2 — the four-cell readout has exactly one co-present indistinguishable twin + (c−1) distinguishable past, FORCED, observer-free.** By `THM_005_023` (exact Ω-tick-arithmetic, `ω_cell = Ω = 1 − π/(3√2)`, window `π ∈ (15√2/7, 9√2/4]`), the population goes `1 → 2 → 4`, skipping 3, the twins C, D born together at tick 8. By `LEM_010_008` (and `THM_010_010` §2(1)), the twins are **Ω-indistinguishable** — *"their readings are the same — there is no count-gap"* — distinguishable only by `σ`; *which* completions coincide is forced by the tick-arithmetic *"in which no observer / σ / chirality appears,"* so observer-free. By `LEM_010_005`, the four cells carry the strict invariant existence-depth order `A > B > {C = D}` against the I=1 unit (the twins are units, not zeros — TD-040). So in the readout: of the `c = 3` read cells, exactly **one** is the co-present, content-indistinguishable twin, and the other **c − 1 = 2** are the content-distinguishable past `A > B`. ∎(2)

**Step 3 — the count: S_flow = (c−1)/c = 2/3, FORCED, observer-independent.** By `LEM_010_011` (promoted, `F-275`): content-distinguishable read cells = `c − 1`; total read = `c`; so the content-distinguishable fraction = `(c−1)/c`, which at the forced `c = 3` is **2/3**. Observer-independent: the flow-direction is invariant (`THM_010_010`), so **either** twin as the reference gives the same `(c−1)/c` (the other co-present, A, B past). Define **S_flow := this count** — `S_flow = (c−1)/c = 2/3`, **FORCED**, the output of the readout count, never assigned. A forced VALUE-theorem (the `THM_003_012`/`THM_004_019`/`THM_004_011` kind). ∎(3)

**Step 4 — the identity: S_flow IS the invariant flow of time read out (the C4-top generating invariant).** By `THM_010_010`, the arrow of time — the order from fewer to more completed records (the count `I`) — is **INVARIANT** (fixed by non-erasure `LEM_002_013` + the forced monotone `ΔI=+1` `THM_005_009`, `I ∈ ℤ≥1` `THM_006_011`); `ι_or` preserves the count, so the direction is `ι_or`-invariant — *"the same forward for every observer,"* **NOT** the observer-relative `σ`-reference (`σ` is the separate, preserved axis). This `ι_or`-invariant directedness is `THM_010_010`'s **promoted main result** (∎, F-272), and it is the **ground** of this step. The content-distinguishable count of Step 3 is the observer reading that invariant flow (the resolvable fraction of the directed four-cell order); so **S_flow = the invariant flow of time read out**, the C4-top **generating invariant** — the readout's analog of `I` (unit), `c` (count), `η` (edge), in the `DEF_003_015`/`THM_004_019` idiom. (`THM_010_010` §9(viii) — RSM's prior musing — *anticipated* exactly this C4-top placement; it is **named corroboration, not the ground** (P11/TD-020): the step stands on the promoted invariance result, never on the candidate note.) ∎(4)

**Step 5 — Ψ, Λ carry S_flow; FORWARD (the one honest difference from η).** As `E`, `G` carry `c` (`DEF_003_015`) and `D`, `Ω` carry `η` (`THM_004_019`), the readout column's `Ψ` (mass-readout) and `Λ` (readout closure) **carry** S_flow ("not independent constants"). But unlike η — whose carriers `D`, `Ω` were already promoted, so `DEF_004_020` could state the carrying relation — `Ψ` and `Λ` are **un-built** (`|Ψ| = m·Ω` deferred OI-052; `Λ = G_readout` the 4th-column gravity, OI-063 step 3). So the carrying relation is a **named forward pointer**, completed when the carriers exist. This card places only the **top** (the forced value); it is a build-order fact, not a deviation. ∎(5)

**What this proof does NOT do / claim:** no **assignment** of S_flow (the count is the output); no **power law** (`(c−1)/c` is a count ratio, not a power) and no **τ·Ω** (retired); no **σ-typing of the arrow** (the arrow is `ι_or`-invariant — TD-041; `σ` not erased); no **wave/cosine/power** substrate (S_flow is the discrete count; Koide is the gated rendering); no **physical mass/energy/seconds** (TD-019/TD-042 — cells carry no physical property); no **Ψ/Λ value** or **built carrying relation** (forward only; the retired seed `Ψ = m·Ω`, `Λ = τ²`, `S·Ψ = Ω²` NOT re-introduced); no **edit of any promoted card** (the M_rank placement is the separate `DEF_010_014`, which assembles, editing nothing).

## 5) Scope & non-overclaim

Establishes: **S_flow = (c−1)/c = 2/3** as the FORCED C4-top readout invariant (the observer's content-distinguishable count of the twin-merged four-cell readout — `c = 3` `THM_003_012`, the twin-merge `THM_005_023`/`LEM_010_008`/`LEM_010_005`, the count `LEM_010_011`), the readout's generating invariant **as `c` is the C2-top count and `η` the C3-top edge** (`DEF_003_015`/`THM_004_019`), identified as **the invariant flow of time read out** (`THM_010_010` §9(viii)); the carriers `Ψ`, `Λ` named **forward** (un-built). Does NOT: **assign** S_flow; assert any **power law** or re-introduce **τ·Ω**; type the **arrow as σ** (it is `ι_or`-invariant — TD-041); build a **wave/cosine/power** substrate or assert the Koide `Q = 2/3` as anything but the gated aperture rendering (R1/R3/TD-003; RO-G.2 `GATE_008_013`); claim a **physical** mass/energy/seconds (TD-019/TD-042); **assign Ψ/Λ a value** or assert a **built carrying relation** (forward only; OI-052/OI-063); **edit/retire** any promoted card (amendment-lock — the placement is `DEF_010_014`, an assembly); admit a **zero/negative** (2/3 ∈ (0,1); twins are units I=1 — TD-040). Guard references: `THM_010_010` (the invariant flow, `ι_or` vs σ — TD-041), `GATE_008_013` (the RO-G.2 rendering fence), `GUARD_003_009`/`GUARD_004_017` (count discipline — (c−1)/c is a count, not a power), `ROLEDEF_008_014` (the observer role). The M_rank placement (`DEF_010_014`), the Koide rendering, and `Ψ`/`Λ` (the 4th-column gravity) are named reference (P11), built by no part of this card.

## 6) Per-symbol accounting

**Introduced: S_flow** (the C4-top readout invariant; `symbols_introduced: [S_flow]`; R17 collision audit in frontmatter — distinct from `S`, `S_rt` (the retired seed name it renames), `S_res`, `𝒮_Φ`, `S_12`, `Ψ`, `Λ`; registry held no prior `S_flow`). **Consumed as ground (2 edges):** the count `(c−1)/c = 2/3` ← `LEM_010_011`; the invariant flow identity ← `THM_010_010`. **Registry symbols used:** `S_flow` (introduced), `I` (the existence count / unit, via the identity), `c` (the count, in `(c−1)/c`, via `LEM_010_011`). **Named (NOT consumed):** `THM_003_012` (c=3), `THM_005_023`/`LEM_010_008`/`LEM_010_005` (the twin-merge), `ROLEDEF_008_014`/`THM_010_006` (the observer/content roles), `LEM_010_012` (the spectrum sibling), `GATE_008_013` (the rendering fence), `DEF_003_015`/`THM_004_019` (the idiom + cross-link), `DEF_010_014`/`THM_003_016` (the M_rank placement, `DEF_010_014`), the retired seed `S_rt = τ·Ω`, `Ψ`/`Λ` (the FORWARD carriers). **Undeclared: none. No decimal except a [COMPARISON OUTPUT]-marked pairing (P8); no physical/mass/energy symbol; no power law; no zero.**

## 7) Verification (witness SPEC — for the independent witness author; P12: mode EXACT-SYMBOLIC, ℚ; reads live promoted ground, TD-030; GENUINE checks, NO hardcoded booleans, F-256; TD-035 disjoint fields; run lint_witnesses.py)

Witness (exact, ℚ; reads the REAL `LEM_010_011`/`THM_010_010` ground LIVE; modes A–E each flip exactly one check):

1. **S_flow = (c−1)/c = 2/3 FORCED, computed not assigned (Mode A teeth, LOAD-BEARING — F-S07Sf-1).** COMPUTE the content-distinguishable count from the twin-merged readout — `c = 3` (`THM_003_012`) read cells, one co-present indistinguishable twin (`THM_005_023` born-together + `LEM_010_008` tie), `c − 1 = 2` distinguishable past (`LEM_010_005`) — and assert `S_flow = (c−1)/c = 2/3` exactly (matching `LEM_010_011`). Mode A: corrupt `c` (→ 2 or 4) or the twin-merge (no tie ⟹ count changes) ⟹ S_flow moves ⟹ check 1 fails.
2. **S_flow is the ι_or-INVARIANT flow read out, σ not erased (Mode B teeth, LOAD-BEARING — F-S07Sf-2).** Confirm the count is observer-independent — either twin as reference gives the same `(c−1)/c` (`LEM_010_011`; `THM_010_010` arrow fixed by non-erasure, not σ) — and `σ` is NOT erased (the co-present twin `σ`-distinguishable). Mode B: type the arrow as the σ-reference (observer-relative) ⟹ check 2 fails.
3. **NO power law / Koide gated-rendering only (Mode C teeth, LOAD-BEARING — F-S07Sf-3).** Assert the card writes NO `S_flow`-power law (`(c−1)/c` a count ratio) AND the continuous Koide `Q = 2/3` / `b/a = √2` / `a + b·cos` is named-forward only (gated, RO-G.2), not the substrate; `τ·Ω` not re-introduced. Mode C: manufacture a power law / make Koide the substrate / re-introduce τ·Ω ⟹ check 3 fails.
4. **Ψ, Λ un-built / forward (Mode D teeth — F-S07Sf-4).** Assert `Ψ`, `Λ` carry NO value (no `|Ψ| = m·Ω`, no `Λ = τ²`) and NO built carrying relation is asserted (forward only; OI-052/OI-063). Mode D: assign Ψ/Λ a value or assert a built carrying relation ⟹ check 4 fails.
5. **Collision-free / no promoted edit (Mode E teeth — F-S07Sf-5).** Assert `S_flow` is registered collision-free (≤ 1 registration, to THIS card; **NEVER "absent"** — absent is true at draft, false at promotion, the `THM_004_019` Mode-E lesson) AND no promoted card is edited (byte-match HEAD). Mode E: reuse an occupied glyph or edit a promoted card ⟹ check 5 fails.

6. **Can-fail (--corrupt), REQUIRED — bare `--corrupt` → the value Mode A; each mode flips ONE check (TD-035).** A→1 … E→5. Plus card-resolution (THM_010_013 + its 2 deps, eq_spine-first). **Run the full mode×check matrix; assert the bijection A→1…E→5; assert normal = 0.** Modes A/B read live promoted ground (TD-030). **Falsifier-signing discipline (TD-041):** confirm each load-bearing falsifier (F-S07Sf-1/2/3) fires when the claim is FALSE, not when TRUE. P8-exact ℚ (Fraction). Run `lint_witnesses.py`; `last_pass_hash: null`; run `promote_card.py --dry-run` as the final pre-gate check.

A pass discharges the structure (S_flow = (c−1)/c = 2/3 forced; the invariant-flow identity; NO power law, Koide gated-rendering only; Ψ/Λ forward; S_flow fresh, no promoted edit). It places only the top (the value forced); the M_rank assembly is `DEF_010_014`. Human-gated at the RSM gate.

## 8) Falsifiers

As frontmatter — **F-S07Sf-1** (S_flow ≠ (c−1)/c = 2/3 / assigned — Mode A, LB, the value), **F-S07Sf-2** (not the invariant flow / typed as σ — Mode B, LB, the identity), **F-S07Sf-3** (power law / continuous-Koide substrate / τ·Ω — Mode C, LB, the fence), **F-S07Sf-4** (carrying relation overclaimed / Ψ,Λ valued — Mode D), **F-S07Sf-5** (collision / promoted-card edit — Mode E). A THM with constitutive falsifiers (D-025.R25): the value can lose (if the twin-merged count is not (c−1)/c, or not 2/3 at c=3) and the identity can lose (if the arrow is the σ-reference, not the invariant directedness). **Falsifier-signing (TD-041):** each load-bearing falsifier is signed to fire when the claim is FALSE (the F-272/LEM_S07_024 lesson). Loss handling per D-025.R26/D-042: the consumed promoted facts (`LEM_010_011`/`THM_010_010`) hold; a violation diagnoses THIS card (a mis-counted readout, a σ-typed arrow, a manufactured law), never a foundational law. **Reopen:** an RSM re-ruling on the C4-top, or a change to the four-cell readout / twin-merge upstream.

## 9) Notes

**(i) Derive, not assign (the FORCED value).** `S_flow = (c−1)/c = 2/3` is the output of the twin-merged readout count (`LEM_010_011`, promoted; `c = 3` forced `THM_003_012`; the twin-merge forced `THM_005_023`/`LEM_010_008`/`LEM_010_005`) — no other value admissible. The witness COMPUTES it (corrupting `c` or the twin-merge moves it), never hardcodes `2/3` (TD-030).

**(ii) The identity is the invariant flow read out — grounded in THM_010_010's PROMOTED MAIN result, not its §9(viii) musing.** S_flow is the **invariant flow of time** (`THM_010_010`: the count's monotone, `ι_or`-invariant directedness — fixed by non-erasure + ΔI=+1, the same for every observer) read out as the content-distinguishable count. This identity rests on `THM_010_010`'s **promoted main theorem** (the arrow IS the `ι_or`-invariant directedness of `I`; ∎, F-272) — load-bearing, gauntleted ground. `THM_010_010` §9(viii) (RSM's prior musing, P11/TD-020-tagged "candidate / does NOT assert the placement") merely **anticipated** this C4-top placement; this card cites it as **named corroboration, never as the ground** — the placement is the RSM-directed forward build (RSM 2026-06-22 "match c=3/η=√2; S07 go"), standing on the promoted invariance result, not on a candidate note (the TD-020 forward-justification fence held). The arrow is **NOT** `σ` (the observer-relative reference, the separate preserved axis) — TD-041, the F-272/LEM_S07_024 lesson held.

**(iii) The one honest difference from η — the FORWARD carrying relation.** η's placement (`DEF_004_020`) could state *"D, Ω carry η"* because `D`, `Ω` were promoted. Here `Ψ` (`|Ψ| = m·Ω`, deferred OI-052) and `Λ` (`G_readout`, the 4th-column gravity, OI-063 step 3) are **un-built**, so *"Ψ, Λ carry S_flow"* is a **named forward pointer**, not a built relation. The C4-**top** is placed now, fully forced; the carrying relation follows when the carriers exist. A build-order fact, not a deviation from the c/η structure.

**(iv) No power law — all counting.** `(c−1)/c` is a **count ratio** (two distinguishable of three — the `k−1` count precedent), NOT a power/quadratic — exactly as the C2 `c²` is "two applications, not a squaring" (`DEF_003_014`/TD-019) and the C3 edge is the counted minimal norm (`THM_004_019`). No `S_flow`-power law is written.

**(v) The rendering fence (R1/R3/TD-003) — mirrors η exactly.** `S_flow = 2/3` is the DISCRETE readout count. The continuous Koide `Q = 2/3` (`b/a = √2`, `a + b·cos`) is the APERTURE RENDERING of this same count (forward, gated RO-G.2 / `GATE_008_013`; derive-then-compare, DD-3) — the old Koide work survives there as the rendered form, never the substrate. No `a + b·cos`, no `(Σ√m)²` power-as-substrate. This is the readout twin of `THM_004_019`'s η-edge rendering fence (the geometry's `b/a = √2` rendering).

**(vi) The 2-of-3 cross-link to η (named, not load-bearing).** Geometry's 2-of-3 — the `(±1,±1,0)` "two channels coupled, one idle" edge `η = √2` (`THM_S03_024`/`THM_004_019` §9(vi)) — and the readout's 2-of-3 — `S_flow = (c−1)/c = 2/3` — are why the two top-row invariants line up for the Koide rendering. `THM_004_019` §9(vi) named this from the η side; this card is its C4 counterpart. Named, not used as a derivation.

**(vii) The retired seed (RSM 2026-06-22 'it is no longer τ·Ω').** The seed value clause `S_rt = τ·Ω` (with `S_rt·Ψ = Ω²`, `Λ = τ² = 4π²`, `Ψ = m·Ω`) is **retired** — no promoted card grounds `τ·Ω` as the flow (`reports/2026-06-22_invariant_flow_of_time_findings.md`: the flow's rate is `Ω`, `THM_005_019`; `τ·Ω` is at most a derived extent-rate, never "the flow"). `S_flow` renames the seed `S_rt` (S_rt→S at `LEM_010_008`, S→S_flow here); the seed value is superseded by the forced count `(c−1)/c = 2/3`.

**(viii) Primitive-violation sweep (CLAUDE.md §8) — clean.** `S_flow = 2/3 > 0` (a count fraction in (0,1)); `c − 1 = 2` is a positive count (the `k−1` count precedent, not a signed quantity); no negative/zero object (the twins are units I=1 — TD-040); no signed arithmetic; no power-as-substrate (R3); no wave/cosine substrate (R1); no physical/time bridge (TD-019/TD-031/TD-032 — S_flow is the internal readout count, the physical match gated RO-G.2); no σ-erasure (σ the separate preserved axis — TD-041); `2/3` exact rational (P8). `S_flow` a fresh glyph (the retired `S_rt` renamed).

**(ix) Forward / fences held.** The M_rank placement (`DEF_010_014`), the Koide `Q = 2/3` rendering (gated RO-G.2), `Ψ` (`|Ψ| = m·Ω`, OI-052), and `Λ` (`G_readout`, the 4th-column gravity, OI-063 step 3) stay OPEN/forward, built by no part of this card. `LEM_010_011`, `THM_010_010`, and all named promoted cards are cited, **never edited** (amendment-lock). No value declared; no power law; no zero; no physical reading.
