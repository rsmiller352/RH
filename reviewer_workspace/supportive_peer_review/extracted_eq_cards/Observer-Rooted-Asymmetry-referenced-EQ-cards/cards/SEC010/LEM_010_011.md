---
id: LEM_010_011
publication_dependencies: ["LEM_010_005","LEM_010_008","ROLEDEF_008_014","THM_005_023","THM_010_006","THM_010_010"]
---

# The observer's content-distinguishable count — the flow of time is INVARIANT (THM_010_010; same direction regardless of observer), and the OBSERVER INDUCES THE READOUT-ASYMMETRY (ROLEDEF_008_014; the readout from its reference at the present). Reading from the present — the INVARIANT LEADING EDGE of the flow (the now, LEM_010_008) — the co-present cell (the simultaneous twin) is content-indistinguishable; the c−1 PAST cells (A>B, LEM_010_005) are content-distinguishable. Exactly ONE co-present (the 2→4 twins, THM_005_023); c=3 (THM_010_006). So the content-distinguishable fraction is (c−1)/c = 2/3. DERIVED from the frame, NOT matched: the Koide identification (this 2/3 = the lepton Σm/(Σ√m)²) is the GATED RO-G.2 derive-then-compare test (GATE_008_013). The flow-direction is invariant regardless of observer; the observer induces the readout-asymmetry, never the direction.

## 0) Governance & Scope Lock

**LEM card (the assemble-pattern) — the observer's content-distinguishable count = (c−1)/c = 2/3.** Two promoted facts, kept distinct (the F-272-safe split):

- **THE INVARIANT — the flow of time** (`THM_010_010`): the order from fewer to more completed records, the **same direction regardless of which observer is chosen** (observer-independent). The order is consumed from `THM_010_010` + the clockhood RO-C (`ROLEDEF_008_004`, recovered from the count) — **NOT** from RO-A (`DEF_008_002` stays unordered-as-membership; the RO-A↔RO-C separation is untouched).
- **THE OBSERVER INDUCES THE READOUT-ASYMMETRY** (`ROLEDEF_008_014`): the observer induces the **readout-asymmetry** — its reference/standpoint (σ) — **never** the flow's direction.

Reading from **the present** — the **invariant leading edge** of the flow (the now, `LEM_010_008`; the just-born twins, `THM_005_023`):
- the **co-present** cell (the other simultaneous twin, at the same leading edge) is **content-indistinguishable** from the reference (it shares the present; distinguishable only by σ — the separate arrow, `THM_010_010`);
- the **c−1 = 2 PAST** cells (A>B — `LEM_010_005`, earlier in the invariant flow) are **content-distinguishable**;
- **exactly ONE** co-present (the 2→4 jump births exactly two at the leading edge — `THM_005_023`); the read set is the **c=3** circulation (`THM_010_006`).

→ the content-distinguishable fraction is **(c−1)/c = 2/3** at the forced c=3, **observer-independent** (the flow-direction is invariant; the observer induces the readout-asymmetry, never the direction; either twin as reference → 2/3).

**DERIVED from the frame, NOT matched.** The Koide identification (this 2/3 = the lepton `Σm/(Σ√m)²`) is the **GATED RO-G.2 derive-then-compare test** (`GATE_008_013`), never on-card (P11). The discrete count is **primary**; the continuous functional is its **faithful aperture rendering** (a rational — no pixelation residual; Unifying_Aperture §9.1). **Gravity-free.** Banned: a count ≠ (c−1)/c; an **observer-relative flow-direction** (the F-272 fence); ≠1 co-present; content/σ conflation; matching Koide on-card; a smooth substrate; amending RO-A; zero/dimensionless; gravity. The **mass spectrum** (GAP-2) is the SEPARATE next step.

## 1) Plain language

Koide's 2/3 is a discrete **count**, read off the pixelated substrate (the smooth Koide formula is only the aperture-rendered picture of it). Here is the count, and the two pieces you have to keep apart.

**The flow of time is the invariant.** There's a definite direction — fewer records → more records, "earlier → later" — and it's the **same for every observer** (that's the arrow card, `THM_010_010`). Nobody's choice changes which way time runs.

**The observer induces the readout-asymmetry.** What the observer *does* supply is its **standpoint** — its "here/now," the reference it reads against. That's the observer's contribution (the readout asymmetry, σ). It changes what the observer distinguishes; it never changes the flow's direction.

Now read from **the present** — the leading edge of that invariant flow (the latest records, the just-born twins, the "now"). Of the three cells the observer reads:
- one is the **other twin** — born at the very same moment, sharing the observer's present. By *content* (how far along in the flow — i.e. mass) it reads the **same**: **indistinguishable**. (Tell-apart-able only by *which side* — σ — but that's the separate arrow, not content.)
- the other two are the **past** — earlier in the flow, plainly different. **Distinguishable.**

So the observer distinguishes **two of three** → **2/3**. The "missing" third is the cell *simultaneous with the observer*. And because the flow's direction is invariant, it doesn't matter which twin is the reference — the other is always co-present, the past is always past — **2/3 either way**.

Two disciplines: this card *derives the count* 2/3; it does **not** claim "2/3 = the Koide mass relation" — that comparison is a separate, gated test. And the order is read from the **flow** (`THM_010_010`) and the **clockhood**, not by re-ordering the readout availability set (RO-A stays as it is).

## 2) Statement

> **(LEM_010_011 — the observer's content-distinguishable count.)** In the realized four-cell frame (`THM_005_023`; within the one existence, I=1), with the flow of time INVARIANT (`THM_010_010`) and the observer inducing the readout-asymmetry (`ROLEDEF_008_014`):
>
> - **(1 — the invariant flow; the present.)** The order from fewer to more completed records is **INVARIANT** — the same direction regardless of observer (`THM_010_010`; the order consumed from `THM_010_010` + the clockhood RO-C `ROLEDEF_008_004`, not from RO-A). The **present** is the **leading edge** of this flow — the latest records, the **now** (`LEM_010_008`; the just-born twins, `THM_005_023`).
> - **(2 — the observer induces the readout-asymmetry, from the present.)** The observer's reference is at the present (the leading edge); by `ROLEDEF_008_014` it **induces the readout-asymmetry** (its standpoint, σ) — never the flow's direction. It reads the **c=3** circulation (`THM_010_006`: the observer the 4th).
> - **(3 — exactly one co-present, content-indistinguishable.)** Of the c=3 read cells, **exactly ONE** is **co-present** with the reference — the other simultaneous twin (`THM_005_023`: exactly two at the leading edge). It is at the reference's present (`LEM_010_008` the tie), hence **content-indistinguishable** (distinguishable only by σ — the separate arrow, `THM_010_010`).
> - **(4 — the c−1 past, content-distinguishable.)** The other **c−1 = 2** read cells (the past, A>B — `LEM_010_005`, earlier in the invariant flow) are **content-distinguishable** from the reference.
> - **(5 — the count.)** So the observer's **content-distinguishable count** is **c−1**, the fraction **(c−1)/c**, which at the forced c=3 is **2/3**. **Observer-independent:** the flow-direction is invariant, so either twin as the reference gives the same (c−1)/c (the other co-present, A,B past).
>
> **DERIVED from the frame** (the invariant flow + the observer-induced readout-asymmetry + the leading-edge present + exactly-1-co-present + the past order + c=3), **gravity-free**; **NOT matched** — the Koide identification is the **gated RO-G.2** test (`GATE_008_013`), the continuous functional the faithful aperture rendering of this rational count. **NO new glyph**; no magnitude; RO-A untouched; the mass spectrum (GAP-2) forward; no zero; no smooth substrate.

## 3) Input contract (GR-1)

**Consumed (6 edges):** the INVARIANT flow / direction + the present-as-leading-edge ← `THM_010_010`; the observer **induces the readout-asymmetry** (not the flow) ← `ROLEDEF_008_014`; the 2→4 jump / exactly-two-at-the-leading-edge (the twins) ← `THM_005_023`; the invariant **now** = the leading edge + the co-present tie ← `LEM_010_008`; the **past** order A>B (the c−1 distinguishable) ← `LEM_010_005`; the **c=3** read structure (the observer the 4th) ← `THM_010_006`. **Named (NOT a dependency — carried / complement / forward):** `ROLEDEF_008_004` (clockhood RO-C — the succession/order recovered from the count, with `THM_010_010`; carried); `DEF_008_002` (RO-A — UNORDERED-as-membership, the RO-A↔RO-C separation, **UNTOUCHED**); `THM_003_012` (c=3 FORCED — via THM_010_006); `AX_001_001`/`THM_002_007`/`GUARD_001_008` (unit I=1 / no zero); `THM_006_011` (I∈ℤ≥1, via THM_010_010); `THM_010_010` also the content-vs-σ split; `DEF_003_003`/`LEM_010_009` (mass = the content/density); `GATE_008_013` (Koide RO-G.2 — gated); `DEF_002_002`/`THM_007_005` (σ); `GUARD_001_005` (no σ²); `LEM_002_006` (no grid); `DEF_005_012` (k≠n≠t); the FCC √2 / N_mass (the mass spectrum, GAP-2 — forward); the continuous Σm/(Σ√m)² (the aperture rendering); the Λ/G_readout — forward. **Domains:** the four-cell frame; the invariant flow; the leading-edge present; existence-depth (content); the observer's reference. **No observer-relative flow-direction; no count ≠ (c−1)/c; no ≠1 co-present; no content/σ conflation; no Koide-match on-card; no smooth substrate; no RO-A amendment; no zero; no gravity; no signed quantity.**

## 4) Proof (complete, structural — the assembled count)

**Step 1 — the invariant flow; the present is its leading edge.** By `THM_010_010`, the order from fewer to more completed records is **INVARIANT** — the same direction regardless of observer (the order recovered from the count, clockhood RO-C `ROLEDEF_008_004`; its direction invariant, `THM_010_010`; NOT from RO-A — `DEF_008_002` stays unordered-as-membership). The **present** is the **leading edge** — the latest records — which is the **now** (`LEM_010_008`: the just-born twins' shared closure-event, `THM_005_023`, the youngest). ∎(1)

**Step 2 — the observer induces the readout-asymmetry, from the present.** By `ROLEDEF_008_014`, the observer induces the **readout-asymmetry** (its reference/standpoint, σ) — and its standpoint is the present (it reads from the leading edge, the now). It does **not** set the flow's direction (invariant — `THM_010_010`); it induces only the readout-asymmetry of *what it distinguishes*. It reads the **c=3** circulation (`THM_010_006`: the observer the 4th). *(The F-272-safe split: invariant flow-direction vs observer-caused readout-asymmetry — two different objects.)* ∎(2)

**Step 3 — exactly one co-present, content-indistinguishable.** The reference is at the present (the leading edge). By `THM_005_023`, **exactly two** cells sit at the leading edge (born together — the twins); the reference is one, so **exactly ONE** read cell is **co-present** (the other twin). It is at the reference's present (`LEM_010_008` the tie), so by `ROLEDEF_008_014` (a state is distinguishable *from* the reference iff it differs) it is **content-indistinguishable** from the reference — distinguishable **only by σ** (the separate arrow, `THM_010_010`), NOT by content (mass = the content/density, `DEF_003_003`/`LEM_010_009`). ∎(3)

**Step 4 — the c−1 past, content-distinguishable.** The remaining **c−1 = 2** read cells are the **past** (A>B — `LEM_010_005`), earlier in the invariant flow (fewer records), at different existence-depths, hence **content-distinguishable** from the present-reference. ∎(4)

**Step 5 — the count, and observer-independence.** Content-distinguishable read cells = c−1; total read = c; so the **content-distinguishable fraction = (c−1)/c**, which at the forced c=3 (`THM_010_006`/`THM_003_012`) is **2/3**. **Observer-independent:** the flow-direction is invariant (`THM_010_010`), so which twin is the reference (σ — observer-caused, `THM_010_010`) gives the same count (the other co-present, A,B past). The count is the assembled consequence of the invariant flow + the observer-induced readout-asymmetry + exactly-1-co-present + c=3. ∎(5)

**Derived, not matched (the discipline).** The count (c−1)/c = 2/3 is derived **from the frame** — gravity-free, with **no continuous wave/power-balance** as the substrate (the discrete count is primary; the continuous `Σm/(Σ√m)²` is its faithful aperture rendering — a rational carries no pixelation residual). The card does **NOT** assert this 2/3 = the lepton Koide Q; that is the **gated RO-G.2 test** (`GATE_008_013`). And the order is sourced from RO-C/`THM_010_010`, **not** RO-A (untouched — TD-033). ∎

**What this proof does NOT do / claim:** no count ≠ (c−1)/c; no **observer-relative flow-direction** (invariant — `THM_010_010`; the F-272 fence); no ≠1 co-present (`THM_005_023`); no content/σ conflation (`THM_010_010`); no **Koide value matched on-card** (gated RO-G.2 — P11/TD-021); no **smooth/continuous substrate** (R1/R2/R3); no **RO-A amendment** (the order is at RO-C/`THM_010_010` — TD-033); no **mass spectrum** (GAP-2, forward); no **gravity**; no **zero/dimensionless** (TD-040); no **σ²**/signed quantity (`GUARD_001_005`).

## 5) Scope & non-overclaim

Establishes: the observer's **content-distinguishable count = (c−1)/c = 2/3**, read from the present (the **invariant leading edge** of the flow, `THM_010_010` / the now `LEM_010_008`), the co-present (simultaneous) cell content-indistinguishable, the c−1 past distinguishable; **forced** and **observer-independent** (the flow-direction invariant; the observer induces the readout-asymmetry, not the direction); **derived from the frame, gravity-free**. The discrete count is primary; the continuous Koide functional its faithful aperture rendering. Does NOT: make the **flow-direction observer-relative** (the F-272 fence — `THM_010_010`); assert the count = the lepton **Koide Q** / match a value (the GATED RO-G.2 test, `GATE_008_013` — P11/TD-021); admit **≠1 co-present** (`THM_005_023`); conflate **content with σ** (`THM_010_010`); use a **smooth/continuous substrate** (R1/R2/R3); **amend RO-A** or source the order from RO-A (TD-033 — the order is at RO-C/`THM_010_010`; `DEF_008_002` untouched); build the **mass spectrum** (GAP-2, forward); route through **gravity**; admit a **zero/dimensionless** state (TD-040) or a **signed** quantity (P3/TD-034). Guard references: `THM_010_010` (the invariant flow / the F-272-safe direction), `ROLEDEF_008_014` (observer induces the readout-asymmetry), `THM_005_023` (exactly-2-at-the-leading-edge), `LEM_010_008` (the now/present), `LEM_010_005` (the past order), `THM_010_006`/`THM_003_012` (c=3), `GATE_008_013` (Koide gated), `DEF_008_002` (RO-A untouched), `GUARD_001_005` (no σ²), `THM_002_007`/`GUARD_001_008` (unit/no zero). The Koide value-match, the continuous rendering, the mass spectrum, and the spacetime reading are named reference (P11), not this card.

## 6) Per-symbol accounting

**Introduced: none** (`symbols_introduced: []`; (c−1)/c is a derived ratio of the registered count c; "the present / the leading edge", "the now", "the co-present cell", "the past cells", "content-distinguishable" are card-local). **Consumed as ground (6 edges):** the invariant flow / the present-as-leading-edge ← `THM_010_010`; the observer induces the readout-asymmetry ← `ROLEDEF_008_014`; the twins / exactly-two-at-the-leading-edge ← `THM_005_023`; the now = the leading edge + the co-present tie ← `LEM_010_008`; the past order ← `LEM_010_005`; the c=3 read structure ← `THM_010_006`. **Registry symbols used:** c (the channel count = 3, `THM_003_012` via `THM_010_006`; the count (c−1)/c → 2/3); I (the existence count / unit, I=1; the invariant flow is the order of I, `THM_010_010`; the present = max I); σ (the orientation — the observer's reference-asymmetry / the co-present's σ-distinguishability, the separate arrow `THM_010_010`). **Named (NOT consumed):** `ROLEDEF_008_004` (clockhood — the order, with THM_010_010), `DEF_008_002` (RO-A — UNTOUCHED), `THM_003_012`, `AX_001_001`/`THM_002_007`/`GUARD_001_008`, `THM_006_011`, `DEF_003_003`/`LEM_010_009`, `GATE_008_013`, `DEF_002_002`/`THM_007_005`, `GUARD_001_005`, `LEM_002_006`, `DEF_005_012`, the FCC √2 / N_mass (GAP-2), the continuous Σm/(Σ√m)², the Λ/G_readout. **Undeclared: none. No magnitude built (the Koide value-match + mass spectrum gated/forward); the only number is the exact rational (c−1)/c = 2/3 (a count); no physical mass/E/κ; no zero, no dimensionless quantity.**

## 7) Verification (witness SPEC — for the witness author; P12: this card's author writes no script; mode STRUCTURAL — reads live promoted ground, TD-030; GENUINE checks, NO hardcoded booleans, F-256; resolve cards eq_spine-first; TD-035 disjoint fields; run the full matrix; run lint_witnesses.py)

Witness (structural; reads the REAL `THM_010_010`/`ROLEDEF_008_014`/`THM_005_023`/`LEM_010_008`/`LEM_010_005`/`THM_010_006` ground LIVE; modes A–F each flip exactly one check):

1. **The count = (c−1)/c from the present-readout of the invariant flow (Mode A teeth, LOAD-BEARING — F-S07L26-1, the hinge).** Model the four cells with count-positions (THM_S04_029/LEM_S07_023 live: A>B>{C=D}, twins at the leading edge); set the reference at the present = the leading edge of the invariant flow (THM_010_010 + LEM_010_008 live). COMPUTE: the co-present cell (at the reference's leading-edge present) is content-indistinguishable; the c−1 past (earlier in the flow) distinguishable ⟹ count = (c−1)/c = 2/3; assert OBSERVER-INDEPENDENT (the flow-direction invariant ⟹ either twin as reference ⟹ same). Mode A: count ≠ (c−1)/c / reference ≠ the leading edge / co-present not the indistinguishable one ⟹ check 1 fails.
2. **The flow-direction is INVARIANT, not observer-relative (Mode B teeth, LOAD-BEARING — F-S07L26-2, the F-272 fence).** Read THM_010_010 live (the arrow invariant). Assert `flow_direction_invariant == True`, `flow_direction_observer_relative == False`; the observer induces the readout-asymmetry (σ), not the direction. Mode B: make the flow-direction observer-relative ⟹ check 2 fails.
3. **Exactly ONE co-present (Mode C — F-S07L26-3).** Read THM_005_023 live (exactly two at the leading edge). Assert exactly 1 read cell co-present. Mode C: 0 or ≥2 co-present ⟹ check 3 fails.
4. **Content NOT σ (Mode D — F-S07L26-4).** Read THM_010_010 live (the co-present σ-DISTINGUISHABLE). Assert the count is content distinguishability; the co-present is σ-distinguishable but content-indistinguishable. Mode D: distinguish the co-present by content / the past by σ ⟹ check 4 fails.
5. **Koide NOT matched / no smooth substrate (Mode E — F-S07L26-5).** Assert the card derives the internal count, does NOT assert "= the lepton Koide Q" (gated RO-G.2, GATE_008_013); the 2/3 is a discrete count, not from a continuous wave/power-balance. Mode E: match Koide on-card / derive via a smooth substrate ⟹ check 5 fails.
6. **RO-A untouched / order from RO-C / no zero / observer-induces-asymmetry-not-flow / no gravity (Mode F — F-S07L26-6).** Assert `amends_RO_A == False` (DEF_008_002 untouched; order from RO-C/THM_S07_024 — TD-033); `zero_admitted == False` (cells units I=1 — GUARD_001_008 live); `observer_creates_flow == False` (induces the readout-asymmetry, not the flow — ROLEDEF_008_014 live); no G/Λ/G_readout. Mode F: amend RO-A / source order from RO-A / admit a zero / observer-creates-flow / route through gravity ⟹ check 6 fails.

7. **Can-fail (--corrupt), REQUIRED — bare `--corrupt` → the hinge Mode A; each mode flips ONE check (TD-035).** A→1 … F→6. Plus card-resolution (LEM_010_011 + its 6 dependency edges + the named cards read live — THM_010_010 for content-vs-σ, eq_spine-first / `.md.promoting`-tolerant). **Run the full mode×check matrix; assert the bijection A→1…F→6; assert normal = 0.** Modes A/B/C/D read live promoted ground (TD-030). Run `lint_witnesses.py`. `last_pass_hash: null`; run `promote_card.py --dry-run` as the final pre-gate check.

A pass discharges the forced count (the present-readout of the invariant flow distinguishes c−1 past from 1 co-present ⟹ (c−1)/c = 2/3; observer-independent; flow-direction invariant; content not σ; derived not matched; RO-A untouched; gravity-free). It builds no value-match, no mass spectrum, no bridge. Human-gated at the RSM gate.

## 8) Falsifiers

As frontmatter — **F-S07L26-1** (count ≠ (c−1)/c / present not the leading edge / co-present not indistinguishable — Mode A, LB, the hinge), **F-S07L26-2** (flow-direction made observer-relative — Mode B, LB, the F-272 fence), **F-S07L26-3** (≠1 co-present — Mode C, LB), **F-S07L26-4** (content/σ conflated — Mode D), **F-S07L26-5** (Koide matched on-card / smooth substrate — Mode E), **F-S07L26-6** (RO-A amended / order mis-sourced / zero / observer-creates-flow / gravity — Mode F). A LEM with constitutive falsifiers (D-025.R25): the forced count can lose (if the co-present cell is shown content-distinguishable, or ≠1 co-present, or the flow-direction shown observer-relative). Loss handling per D-025.R26/D-042: the consumed promoted facts hold; a violation diagnoses the count (a wrong present, a wrong co-present count, a content/σ conflation, an observer-relative flow, a matched value), never a foundational law. **Reopen:** the Koide RO-G.2 comparison (the gated derive-then-compare), the mass spectrum (GAP-2), or an RSM re-ruling.

## 9) Notes

**(i) The F-272-safe split (RSM 2026-06-21).** THE INVARIANT is the flow of time — the same direction regardless of observer (`THM_010_010`, the object F-272 protects). THE OBSERVER INDUCES THE READOUT-ASYMMETRY — the readout-asymmetry (its reference, σ; `ROLEDEF_008_014`). The observer's choice changes the asymmetry it reads, NEVER the flow's direction. This is the *correct* role of the observer (it induces the readout-asymmetry); the F-272 error was making the *flow* observer-relative — which this card explicitly forbids (F-S07L26-2).

**(ii) The order is at RO-C/`THM_010_010`, not RO-A (TD-033).** The content's order is the invariant flow (`THM_010_010`) + the clockhood succession (RO-C, `ROLEDEF_008_004`, recovered from the count). RO-A (`DEF_008_002`) stays UNORDERED-as-membership — the deliberate RO-A↔RO-C separation, **untouched** (no amendment). (The earlier obstruction report's "amend RO-A" conclusion is superseded: the order was already at RO-C/`THM_010_010`.)

**(iii) Why exactly (c−1)/c.** Exactly ONE cell is co-present with the present-reference — the 2→4 jump births exactly **two** at the leading edge (`THM_005_023`; reference one, co-present the other). The other c−1 are the past (`LEM_010_005`). At c=3 (`THM_010_006`/`THM_003_012`): (3−1)/3 = 2/3.

**(iv) Content, not σ (`THM_010_010`).** The co-present twin is **content (depth/the flow / mass) indistinguishable** but **σ-distinguishable** (the observer-induced readout-asymmetry, the separate arrow). Koide is a mass relation (content); the count is content-distinguishability. σ is the observer-induced readout-asymmetry, not the count.

**(v) Derived, not matched (P11/TD-021); the discrete count is primary.** The card derives the dimensionless count from the frame; it does **not** assert "2/3 = the lepton Koide Q" — that is the **gated RO-G.2 derive-then-compare test** (`GATE_008_013`). The discrete count is primary; the continuous `Σm/(Σ√m)²` is its **faithful aperture rendering** (a rational carries no pixelation residual — Unifying_Aperture §9.1; R1/R2/R3).

**(vi) Gravity-free; the mass spectrum is the separate next step (GAP-2).** The count routes through no gravity. The individual masses (e/μ/τ — the FCC √2 connectivity / the integer maintenance-loads N_mass) are the **separate** next step (GAP-2), forward.

**(vii) Primitive-violation sweep (CLAUDE.md §8) — clean.** c is the framework channel count (=3); (c−1)/c a ratio of strictly-positive counts; σ the discrete orientation (no σ², `GUARD_001_005`); the invariant flow is the monotone order of the licensed count I (not a free scalar); no zero / dimensionless (cells units I=1 — TD-040); no physical mass/energy/κ (TD-019); no smooth substrate (R1/R2/R3); no background grid (`LEM_002_006`); π absent (the only number is the exact rational 2/3).

**(viii) Forward / fences held.** The Koide value-match (RO-G.2, `GATE_008_013`), the continuous Σm/(Σ√m)² rendering, the mass spectrum (GAP-2, the FCC √2 / N_mass), and the Λ/G_readout spacetime reading stay OPEN/forward, built by no part of this card. RO-A (`DEF_008_002`) untouched. No new glyph, no magnitude, no value-match, no zero, no smooth substrate, no gravity. All promoted cards named, never edited.
